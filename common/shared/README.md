# common/shared

**禁止未核准抽 shared／Architectury。** 此目錄為空殼佔位，不是現行 runtime 源碼樹。

Reserved for pure-ish logic extracted later (MinPlay+) — only after an explicit extract PR.

**Skeleton:** intentionally empty of Java sources. Do not move `LlmClient` / config-bound classes here until `PackAiLog` + `PackAiSettings` adapters exist.
