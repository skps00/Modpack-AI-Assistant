# Plan C v3 — 用包自己嘅文字回答「點充能／點嚟」（含轉換類條目）

> 狀態：**v3（2026-09-17 12:5x）** — 已寫入 SK 決定（**3y**：連「轉換」類都顯示畀玩家）＋上一輪評審全部修正。
> 開工條件：評審達標（正方 ≥8 : 反方 ≤2）。

## §0 背景（我上一版錯咗咩，唔再重複）
1. 我原本以為「查玩家問嗰件嘢嘅提示文字」就得——**錯**：條件文字係寫喺**來源物品**身上。
   實例：玩家問「滿溢神恩項鍊點充能」→ 4 隻王名係寫喺**空項鍊**（`kubejs.tooltips.god_bless_empty_necklace.1`：击败虚空之花、暗夜巫师、黑曜巨石柱、下界铁掌之一即可充能）；滿溢項鍊自己嘅提示係另一句（講沙漠地牢挑戰）。
2. 我原本以為「轉換」類條目（空項鍊→滿項鍊）已經出到玩家——**錯**：今日過濾只放「產出」類，轉換類一律跳過 → 我上一版嘅旗艦例子永遠唔會出現（SK 已答 **3y**：要放寬）。
3. 我原本假設提示 key 一定係 `kubejs.tooltips.<路徑>.<序號>`——**錯**：真語料有 **61** 條冇序號（例 `kubejs.tooltips.heart`），而且 `kubejs` 前綴只佔一小部分（真語料 13 個前綴、3684 條 key，`kubejs` 只 649），單一前綴硬配命中率只有 46/260。
4. 我原本寫「重疊度 ≥0.6 就唔重複」——**假精確**：真語料 59 對最大重疊只有 0.071，門檻永遠唔觸發。

## §1 交付
1. **放寬可見性**：玩家問嘅物品命中時，「**產出**」同「**轉換**」兩類都顯示（仍然最多 3 行、仍然 fail-closed）。
2. **條件文字用站點來源物品嘅包原文**：站點係「轉換／互換」時查**來源物品**，係「產出」時查產出物品。
3. **答案只講一段**：用決定表（唔用數值門檻）避免同一件事講兩次。
4. **新開關**：`jsObtainPackText`（預設開）＋ 3 個語言檔 ＋ 設定頁接駁（唔可以漏）。

## §2 設計（寫死）
- **D1** 可見性（`PackIndex.java:1271-1276`）：條件由 `kind != PRODUCE → continue` 改為「**PRODUCE 或 TRANSFORM** 都收」（SWAP 暫不收，避免噪音）；仍然 `MAX_JS_OBTAIN_LINES=3` 截斷。
- **D2** 站點要帶來源物品：`JsObtainSites` 嘅站點記錄**加一個欄位**（來源物品 id，今日解析時已經見到——策略 key 就係空項鍊 id；例 `entity_death.js:22-26` 嘅 `'kubejs:god_bless_empty_necklace'`）；產出類則該欄位為空。
- **D3** 文字來源解析順序：① 該物品所屬 namespace 嘅提示前綴 ② `kubejs` 前綴 ③ 其餘前綴；**key 形狀**同時支援 `<路徑>.<序號>` 及**冇序號**；**多個 namespace 同名 → 唔配 ＋ 寫診斷 log**（真例：`flame_heart`、`ice_heart`、`creeper_appendix`）。
- **D4** 去重（決定表）：
 · 程式碼條件係**變數名**（玩家睇唔明，例 `bossesOfMassDestructionBossTypeList`）→ **只用包原文**；
 · 程式碼條件有**數值／機率**（例 `random<0.3`）→ 包原文 ＋ 程式數值**併成一句**；
 · 包冇原文 → 用程式碼字面 → 兩者都冇 → 機械式「來源→觸發→產出」。
- **D5** 顯示期查文字（唔改存檔格式），離線測試用**注入式文字來源**（唔依賴玩家電腦有齊三種語言；實測只裝咗簡中）。

## §3 技術錨點
- 站點解析：`logic/JsObtainSites.java`（今日已有 `cond` 字面、`kind`）；`kubejs/server_scripts/curios/entity_death.js:22-26`（真例）。
- 可見性：`logic/PackIndex.java:1271-1276`（`kind != Kind.PRODUCE → continue`）。
- 文字來源：`logic/PackIndex.java:295`（載語言檔）、`:307`（翻譯表）；顯示期先例 `client/service/AskService.java:2250`、`logic/ModularToolScan.java:277`。
- 文字出口：`logic/ReplyLang.java`（`jsProduce`／`jsSwap`／`jsTransform`）＋新增 `jsObtainCondition(...)`。
- 語言檔真例：`<instance>/kubejs/assets/kubejs/lang/zh_cn.json:1093`（空項鍊條件句）；該包**冇** `en_us.json`／`zh_tw.json`。

## §4 驗收（S1–S9）
- **S1** 旗艦例子（強制 fixture）：站點產出＝滿溢項鍊、來源＝空項鍊 → 條件欄 == 包原文 4 隻王名 → 今日紅（因為轉換類今日被跳過）。
- **S2** 去重決定表：① 變數名條件 → 只出包原文（程式字面唔准出現）② 有 `random<0.3` → 併一句 ③ 冇原文 → 機械式 → 今日紅。
- **S3** key 形狀：有序號（多段串接）＋**冇序號**（`kubejs.tooltips.heart`）兩者都解析到 → 今日紅。
- **S4** 注入式多語：唔假設玩家有齊語言檔（缺 → 機械式，唔准出 raw key）→ 今日紅。
- **S5** 撞名 fail-closed：用真例 fixture → 唔配 ＋ 診斷有紀錄 → 今日紅。
- **S6** 放寬後**冇噪音**：產出類結果不變（回歸）；轉換類最多 3 行、冇來源物品時唔出條件欄 → 今日紅。
- **S7** 舊有檢查全綠（`runAskJsObtainSitesCheck`／`runAcquireFactsCheck`／`runAskMechanicFactsCheck` ＋ python 閘今日 116 通過 0 失敗）→ 綠（防回歸）。
- **S8** 新開關對齊：`jsObtainPackText` 3 語言 ＋ 設定頁接駁檢查 → 今日紅。
- **S9** 真機（SK）：問「滿溢神恩項鍊點充能」→ 答案條件欄出現包原文 4 隻王名。

## §5 唔准做
- 唔准自創動詞（包有字用包原文、包冇用機械式）；唔准單一前綴硬配；唔准用數值門檻做去重；唔准改存檔格式；唔准冇診斷記錄就靜默唔配。

## §6 評審記錄
| 輪 | 對象 | 正方 : 反方 | 結果 |
|---|---|---|---|
| 1 | v1 | 2 : 8 ／ 3 : 7 | 旗艦方向錯（查錯物品）、轉換類被濾走、key 前綴假設錯、去重門檻假精確 |
| 2 | v3（本檔） | 待跑 | 已寫入 SK 3y 決定＋全部修正 |
