```json
{
  "ratio": "正方 3 : 反方 7",
  "verdict": "【結論】方向（窄範圍、harness 先行、紅→綠）合理，但 v4 喺 bug 2 修法、§5 cache/caps 論述、§6-S4 閘、§6-S3 接受標準、§7 rollback 四處有實質錯誤，必須修訂後才可開工。\n\n(1) Peer-scan 修法：唔充分、唔精確。【精確成因（已用真字符串驗證）】ITEM_ID（OfficialDisplay.java:24-25）會喺 `…item_tooltips.js:2` 內只匹配到 `js:2` 這個碎片；resolveAnnotatable（:166-188）永遠取「最尾兩個 colon 段」，所以 3 段命中 `source:kubejs/client_scripts/item_tooltips.js:2` 嘅 ns 被當成檔案路徑 `kubejs/client_scripts/item_tooltips.js`，而 annotatable()（:202-225）嘅 `.js` 排除只檢查 path 半邊（`2`）於是通過；annotatableNamespace（:190-200）嘅 5 條 label 黑名單喺呢個已經錯嘅 ns 上幫唔到手。【plan 寫法不可執行】「剔除 evidence=／source: 尾段同 .js:<n> 形 token」係症狀描述：(a) 裸路徑家族嘅命中本身係 `js:26`（`.` 喺 match 之前、唔喺 match 內），若過濾施加於 m.group() 就完全唔會 fire；(b) 施加於 resolved id 只救得到全程徑家族。而且 annotate()（:87-109）用同一個 predicate，只改 collectPeers 會令 `（無官方名）` 繼續留喺 fact 本體（真 trace event[54]/[56] 見 `(js:2（無官方名） A)`）。【應實作 predicate】P1 token 邊界：m.start() 前一字元或 m.end() 後一字元 ∈ [A-Za-z0-9_./\\-] → 丟（殺 `js:26`）；P2 延伸檔名檢查施加於整個 resolved id（含 ns），非只 path；P3 ns 必須 ^[a-z0-9_.-]+$（唔准 `/`、唔准大寫）——我已掃 instance kubejs 全 4,321 檔，odd namespace = 0，故無真 id 會誤殺；P4 黑名單由 5 條擴到實際出現嘅 fact key／NBT key；P5（peer 行專用）只有 officialName(id) 非空才可做 peer。【真字符串反例】39 條 trace 共 67 個 peer entry，64 個係垃圾：`kubejs/client_scripts/golden_age/item_tooltips.js:1（無官方名）`×24、`mechanic:none（無官方名）`×18（完全冇 `.js`，S4 斷言照樣係綠！）、`整合包脚本（無官方名）`×10（humanize 後亦冇 `.js`）、`none（無官方名）`×6、`kubejs/server_scripts/common/player_login.js:2（無官方名）`×3（裸碎片家族）；唯一合法 peer 係 `彩虹糖果（kubejs:colorful_candy）`×3。NBT：trace 真見 `{Count:1b,id:`（18×）→ 現況會產生 peer `count:1（無官方名）`。另注意真 id 有帶點同斜線嘅（`ino_dlc_build:music.build_henshin` 125 次、`ino_dlc_build:item/item/build_phone` 128 次、`mrqx_extra_pack:item/mystery_item`）→ 唔可以粗暴用「含點／含斜線即丟」。【最小紅→綠證據】Java：AskDisplayNameCheck.java:55-72 現有 fixture 用 `source:ftbquests/quests/demo.snbt`（2 段，ns 剛好 == label「source」→ 意外安全，所以 bug 一直走漏）；最小紅案 = 同一 fixture 加 `:2` 行號 + 抄自 trace 嘅原句，加斷言「peer/fact 內不得出現 `js:`／`.js`／`mechanic:`，peer 不得以（無官方名）結尾」，並保留一個合法 peer 正向對照；跑 `python research/gen_tmp_check.py` → `./gradlew.bat -I tmp-check.gradle runAskDisplayNameCheck` 喺未修 commit 上取得 FAIL 輸出（紅證據），修後 OK。Python：掃 `packai/trace/ask-*.jsonl` peer 行（我已量到 33 個含 `.js:<n>`）——呢個今日已經係紅；plan 寫嘅 `check_ask_display_leak.py` 今日 RC=0（latest.log 內 peer 行 = 0）→ 永遠唔會紅，唔可以當證據。\n\n(2) §5 caps/evidence-ordering/cache：【caps 用錯 pipeline】formatUse/formatDrop（KubeJsMechanicScan.java:1017-1023/1025-1037、clip(…,400)@:1368）屬 KubeJS mechanic-facts 路徑，而新 transform edge 唔經該路徑。若按 plan 加喺 PackIndex.java:1677（ingestGraph 內），真正生效嘅上限係：MAX_GRAPH=200 且 addFact 會靜默早退（PackIndex.java:2541-2546、:28）、retrieve 只讀 40 檔（:510）、acquire 只出 12/3 行（AskToolContext.java:31-33、PackIndex.java:1266）、clipAcquireLines 取頭 N 行（AskToolContext.java:123-141）→ 焦點項嘅 transform edge 必須走 addFactForced/focus-pin 路線（照 pinFocusLootJsFacts :2275）否則可被靜默丟失；plan 完全冇提。【evidence 前置 ≠ 修 bug】真（evidence 尾 append 會被 400 截，clip 無省略號），但位置無關，mangling 喺邊個位置都會發生，所以「evidence 放最前」只係化妝。【§2「零 answer layer 改動」已被推翻】新 `-[transform]->` 喺 AskEngine.java:472-504 冇任何 branch 命中 → 直接被丟；AskPurposeContext.java:48-61 白名單亦唔含 transform；PackIndex.acquireFactsDetailed :1265-1379 只認已知 kind，且 :1385-1387 一 ranked/cycles 全空即回 AcquireFacts.empty()。→ 需要新增 PackIndex ranking branch + ReplyLang 渲染（若仲要入 LLM facts，仲要動 AskEngine/AskPurposeContext），故 §5 自己寫嘅「唔改 AskPurposeContext」同 §6-S3 自相矛盾。【語言硬編】ingestAcquireEdges（PackIndex.java:2249）用 `ReplyLang.humanAcquireLabel(\"zh_tw\", …)` 把 zh_tw 字串烘入 edge；若照 plan 格式把「由『X』於腳本事件轉換」寫入 edge payload，zh_cn/英文 ask 會出 zh_tw 文字（S3 用 zh_cn → 直接對唔上）。【cache】kjs-*.json key = sha256(rel+\"\\0\"+src)（KubeJsMechanicScan.java:1093）——真，無 parser 版本；但新 parser 若放 PackIndex，PackIndex 冇任何磁碟 parse cache（只有 index.json 檔-meta，靠 :505 INDEX_SCHEMA_VERSION 廢），故 plan 所述「要刪 cache」嘅理由對唔上今次改動；手動刪 cache 亦無閘看守 → 建議 :1093 加 PARSER_VERSION 入 hash 自動失效。【rollback 不完整】§7 只有 git revert + 刪 mechanic-cache；缺 (a) instance 入面嘅 jar（AGENTS 部署鐵則 #4：由 %TEMP%\\deploy_backup_*\\ 還原 / 用 mc_mod_deploy_jar.py 重新部署並核 sha256），否則客戶端仍然跑新 parser；(b) 有冇 bump INDEX_SCHEMA_VERSION 要明寫（forward 有 bump 就 revert 亦要再 bump，否則舊 index.json 被靜默重用）；(c) 舊 trace/latest.log 仍含舊漏字 → 新閘要按版本／日期 scope，否則正確 revert 之後閘仍然紅（假失敗）；(d) 冇 config kill-switch（其他來源都有：PackAiConfig.java:152/157/162/188）→ 現場無得關；(e) 未寫 revert 後要重跑 S4 閘（必須喺舊 code 上紅）＋115 check。\n\n(3) S3 誠實性：唔可以照字面驗收，且多處 overclaim。【名係假】pack 只有一個 lang 檔 `kubejs/assets/kubejs/lang/zh_cn.json`：`item.kubejs.god_bless_empty_necklace` = 「神恩项链」(:445)、full = 「满溢神恩项链」(:446)——全 pack 冇「（空）」字樣；「擊敗 boss」只存在於 tooltip 文字 `kubejs.tooltips.god_bless_empty_necklace.1`(:1093「击败虚空之花、暗夜巫师、黑曜巨石柱、下界铁掌之一即可充能」)，而 P1 pattern 唔 parse tooltip → pattern 最多只講到 source item + rel:line（＋若順手抽 event 名就 `EntityEvents.death`）。S3 必須改成喺 trace `send.facts` 上斷言：有 `-[transform]->` + `kubejs:god_bless_empty_necklace` + `curios/entity_death.js:26`；顯示名／「boss」措辭當觀察值，唔做 pass 條件。【玩家唔會見到 entity_death.js:26】Plainify.humanizeText（:265-266）會把 `kubejs/…` 換成「整合包脚本」（trace 真見「整合包脚本（無官方名）」×10）→ 玩家眼裡只會係「整合包脚本:26」；證據只可喺 trace/LLM-facts 層驗。【負對照 #3 係錯】我掃全 kubejs 4,321 檔，`setStackInSlot(…, Item.of(…))` 全 pack 只有兩處：entity_death.js:26 同 entity_hurt.js:36，而後者出 `irons_spellbooks:silver_ring`（普通物品）→ 問 silver_ring 一定會有 transform edge，「問一件普通物品 → 新 edge 數 0」必然紅；要改成「揀一個真係冇 site 嘅 id」，同時要定義「edge 數」點觀察（現時冇 log，只有 trace；建議加 `Pack AI transform edges item=… n=…`）。【真例引用錯】§4 role table 寫 `server_scripts/b_a_d/ritual/summoning_rituals.js:434-440`，但該檔只有 249 行；真 block 喺 `server_scripts/ritual/summoning_rituals.js:433-439`（`:435` `.altar('kubejs:god_bless_full_necklace')`、`:439` `.itemOutput('kubejs:god_bless_empty_necklace')`）——順帶：即係 full necklace 係祭壇輸入、產出 empty，P2 完全冇提呢個真 case。【P2 漏真輸入形】真檔有 `.input('16x minecraft:yellow_dye')`、`.input('8x #minecraft:planks')`（計數前綴 + tag）→ CONSUMES 抽取要剝 `Nx ` 同丟 `#tag`，plan 冇寫。【§7 最壞情況漏了真風險】兩個真 site 都係有條件守門：entity_death 要 boss 類型清單；entity_hurt 要 `item.nbt.friendName` + 好友在線且活住 + 血量 ≤4（救援傳送，順手把戒指變銀戒）。呢類「pattern 真命中但語意上唔係取得途徑」嘅誤導，9 條（集中喺 constdef.js 一個檔）負對照抓唔到；edge 文字唔應該寫成普通取得路徑（要「條件見腳本」/tier 註記）。\n\n【唯一站得住嘅正方】窄範圍決定合理；P1 條件（const 物件 + function value + 單一 item key + setStackInSlot + key≠out）實測精準——全 pack 4,321 檔只有 2 個 site 且兩者都係真 transform；red-before-green 嘅順序要求、harness＋負對照嘅思路、以及 honest-miss 唔亂講嘅原則都對。",
  "required_changes": [
    "OfficialDisplay.java:24-25（ITEM_ID）加 token 邊界：`(?<![A-Za-z0-9_./\\\\-])#?[a-z0-9_]+(?::[a-z0-9_./-]+)+(?![A-Za-z0-9_./-])`；附一個 harness case 證明 `js:26`／`js:2` 唔再 match（真反例：`kubejs/server_scripts/common/player_login.js:2`）。",
    "OfficialDisplay.java:202-225（annotatable）把 `path.contains(\".js|\\.snbt|\\.json\")` 類檢查改為施加於整個 resolved id（含 ns 段），並要求 ns 符合 ^[a-z0-9_.-]+$（唔准 `/`、唔准大寫）；保留 path 內嘅 `.` 合法（真 id：`ino_dlc_build:music.build_henshin`、`mrqx_extra_pack:item/mystery_item`、`ino_dlc_build:item/item/build_phone`）。",
    "OfficialDisplay.java:190-200（annotatableNamespace）黑名單由 5 條（item/source/tier/note/file）擴到實測會漏嘅 fact key／NBT key：mechanic（trace 58 次）、via/held/gets/entity/table/gateway/structure/dimension/from/src/rel，以及唔分大小寫嘅 Count/Damage/tag/id（trace 真見 `{Count:1b,id:` 18 次 → 否則產生 peer `count:1（無官方名）`）。",
    "OfficialDisplay.java:87-160：修法唔可以只喺 collectPeers；annotate()（:87-109）必須共用同一 predicate（否則 `（無官方名）`／`js:2（無官方名）` 繼續留喺 fact 本體，trace event[54]/[56] 已見）；collectPeers（:144-160）另加「peer 必須 officialName 非空」規則（實測 64/67 現有 peer entry 係（無官方名）垃圾，唯一合法者有名）。",
    "AskDisplayNameCheck.java:55-72：把現有 `source:ftbquests/quests/demo.snbt` fixture（2 段、ns 碰巧 ==「source」→ 意外安全，係 bug 走漏嘅原因）加上 `:2` 行號變體 + 抄自 trace 嘅原句 `(source:kubejs/client_scripts/item_tooltips.js:2 tier:A)`，斷言 enrichFacts 輸出內冇 `js:`／`.js`／`mechanic:`、peer 行冇（無官方名），並保留一個合法 peer 正向對照；紅證據 = 修前跑 `python research/gen_tmp_check.py` 後 `./gradlew.bat -I tmp-check.gradle runAskDisplayNameCheck` 嘅 FAIL 輸出。",
    "tests/check_ask_display_leak.py:38-53：加入 peer 行規則；並明確記錄現況係 RC=0、latest.log 內 peer 行 = 0，故此閘唔可以做 S4 證據；真正紅證據要另外掃 `packai/trace/ask-*.jsonl`（實測 33 個 peer entry 含 `.js:<n>`），而且閘要按 build／日期 scope 免舊 trace 永久紅。",
    "PackIndex.java:1265-1379：新增 `-[transform]->`／`-[ritual]->` 嘅 ranking branch + ReplyLang 渲染，並處理 :1385-1387（ranked/cycles 全空 → AcquireFacts.empty()，否則連 header 都冇）。",
    "PackIndex.java:1677-1684／2275-2291：新 parser 呼叫必須走 focus-pin（addFactForced 風格，照 pinFocusLootJsFacts），否則會被 MAX_GRAPH=200（:28、:2541-2546）同 retrieve 只讀 40 檔（:510）靜默丟失——plan §5 完全冇提呢啲真 caps。",
    "PackIndex.java:2249：唔可以把 prose／zh_tw 烘入 edge；edge 保持機器可讀（例如 `-[transform]-> from:<id> src:<rel>:<line>`），喺 ask 時用 replyLang 渲染，否則 zh_cn 嘅 S3 會見到 zh_tw 文字。",
    "AskEngine.java:472-504 + AskPurposeContext.java:48-61：推翻 §2「零 answer layer 改動」——新 edge 目前冇任何 branch 命中會直接被丟；要加 transform kind 路由。同步改 §5 嘅「唔改」清單。",
    "PackAiConfig.java:152-190：為新 fact 來源加 toggle（照 KUBEJS_MECHANIC_SCAN/QUEST_MECHANIC_FACTS/KNOWLEDGE_ENABLED 形式）＋3 檔 lang ＋ `tests/check_settings_registry.py` 綠，否則現場冇 kill-switch。",
    "KubeJsMechanicScan.java:1093：cache key 加 PARSER_VERSION salt 自動失效（sha256(rel+\"\\0\"+src) → 版本化），唔好只靠人手刪 `config/packai/mechanic-cache/`；手動刪仍保留做保險。",
    "plan §3／§4 行號引用改正：`server_scripts/b_a_d/ritual/summoning_rituals.js:434-440`（該檔只有 249 行）→ `server_scripts/ritual/summoning_rituals.js:433-439`；並補 `.input('16x minecraft:yellow_dye')`／`.input('8x #minecraft:planks')` 嘅計數前綴剝除與 `#tag` 丟棄規則。",
    "plan §6-S3 改寫：刪「由神恩項鍊（空）於擊敗 boss 後轉換取得」（lang 檔真名係「神恩项链」，zh_cn.json:445；全 pack 冇「（空）」，「boss」只喺 tooltip :1093）→ 改成喺 trace `send.facts` 斷言 `-[transform]->` + `kubejs:god_bless_empty_necklace` + `curios/entity_death.js:26`；並明寫玩家可見文字會被 Plainify.java:265-266 humanize 成「整合包脚本:26」，故證據只喺 trace/LLM-facts 層可驗。",
    "plan §6-S3 負對照修正：唔可以講「問一件普通物品 → 新 edge 數 0」（`irons_spellbooks:silver_ring` 喺 entity_hurt.js:36 真有 site）；改成指定冇 site 嘅 id，並定義「edge 數」嘅可觀察方式（加 `Pack AI transform edges item=… n=…` log 或掃 trace）。",
    "plan §7 補完 rollback：加 instance jar 還原（由 %TEMP%\\deploy_backup_*\\ 或用 mc_mod_deploy_jar.py 重新部署＋核 sha256）、明寫有冇 bump INDEX_SCHEMA_VERSION（forward 有 bump → revert 亦要再 bump，否則舊 index.json 被重用）、新閘要版本 scope（舊 trace 免假紅），以及 revert 後必須重跑 S4 閘（舊 code 上必須紅）＋115 check 全綠。",
    "plan §3-P1 edge 措辭／§7 最壞情況：兩個真 site 都係有條件（entity_death 要 boss 類型清單；entity_hurt 要 NBT friendName＋好友在線＋血量 ≤4 嘅救援傳送），唔可以渲染成普通取得途徑；edge 文字要加「腳本事件轉換（條件見腳本）」之類註記，並把「pattern 真命中但語意誤導」列入最壞情況（現時 9 條負對照集中喺 constdef.js，抓唔到）。"
  ]
}
```