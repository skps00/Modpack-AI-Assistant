```json
{
  "ratio": "正方 2 : 反方 8",
  "verdict": "【(1) 兩個 pattern 嘅方向／證據審查】\n\nP1（const map 變身）——**方向正確，但設計嘅真正難點唔在 precision 而在 reachability，而且 §5 嘅落地指令會直接產生反方向 edge。**\n· 走真 statement：`server_scripts/curios/entity_death.js:22` key=`'kubejs:god_bless_empty_necklace'`、`:26` `curios.setStackInSlot(slot, Item.of('kubejs:god_bless_full_necklace'))`；`entity_hurt.js:31/36` key=`'kubejs:friend_to_the_end'`、out=`'irons_spellbooks:silver_ring'`。必要條件 3（key≠out）令方向唯一可得，唔會反轉 → 方向 OK。\n· 但條件 1＋2 幾乎係「by construction」：我 grep 全包，符合 `^\\s*'itemid'\\s*:\\s*function` 嘅 id-keyed function map 有 **~150 個檔**（organ/dlc/golden_age/ino_dlc… 每個檔幾十個 key），而全包只有 **2 處** `setStackInSlot(..., Item.of('literal'))`（grep 實測 5 個 setStackInSlot，另外 3 個係 `inventory.setStackInSlot(slot, newItem)`、`inv.setStackInSlot(blanks[i++], new ItemStack(key,max))`）。即 P1 嘅精度係「世界上只有 2 個 site」造成，唔係條件推導出嚟；真正未解嘅係 reachability（見 (3)）。\n· **反方向風險（實證）**：§5 寫「新增 `ingestAcquireEdges(rel, text, \"transform\")` 一次呼叫（`PackIndex.java:1677` 附近）」。但 `ingestAcquireEdges`（`PackIndex.java:2248-2260`）嘅語義係「用 `ITEM` regex 掃成個檔，**每一個** quoted item id 出同一 label 嘅 edge」。照字面重用：entity_death.js 只有 2 個 id → 會同時出 `item:kubejs:god_bless_empty_necklace -[transform]->`（**被消耗嘅 source 竟變「可由轉換取得」**）＋ `item:kubejs:god_bless_full_necklace`；entity_hurt.js 會令 `kubejs:friend_to_the_end` 變成可取得。呢個直接違反 §3 必要條件 3 同 §4 角色表，係 plan 內部自相矛盾。必須寫明係**新函式**（只對抽到嘅 `<out>` emit），唔係重用。\n· 重複：與現有 `-[use]->`／`-[on:…]->` 唔同 label，唔算重複；但同一 item 會多一條取得行，而 SLIM 預算只有 3 行（見 (3)）。\n\nP2（鏈式祭壇產出）——**方向正確，但 §3 嘅 pattern sketch 同真旗艦形狀唔一致，照 sketch 寫一定漏旗艦。**\n· 我用官方 docs 核過方向（AGENTS.md research-first）：`docs.almostreliable.com/summoningrituals/recipe/basics.html` —「`altar()` … takes a single argument, which acts as the **initiator** … must be inserted last into the altar to start the recipe」→ initiator 係**消耗**，`.input()` = item inputs **consumed**；同頁／`outputs.html` — `itemOutputs(...)`（別名 `itemOutput`, `outputs`, `output`）= items **spawned around the altar** → 產出。故 §4 角色表 `altar/input=CONSUMES、itemOutput=OUTPUT` **方向正確**，正方得分。\n· **但 sketch 錯形狀**：§3 寫 `.itemOutput(Item.of('out'))`；真旗艦 `server_scripts/ritual/summoning_rituals.js:439` 係 `.itemOutput('kubejs:god_bless_empty_necklace')` —— 裸字串。實測該檔 8 個 `itemOutput` **全部**係裸字串，全檔 `Item.of` 只出現 1 次。若 parser 依 §3 收 `Item.of('…')`，S1「P2 出到 -[ritual]->」直接 FAIL。\n· 鏈邊界：真鏈 `:434 event.recipes.summoningrituals` 自成一行，上一行係註解 `:433 // 神恩Boss`，再上係上一條鏈嘅 `:432 .recipeTime(200);`；鏈內另有 `.id('kubejs:god_bless_summon')`（:436）、`.blockBelow('minecraft:command_block')`（:437）——`.id`／`.blockBelow` 都會被 `ITEM` regex 命中，靠「只認 altar/input/itemOutput」嘅白名單排除，即 ⑧ 被條件自動涵蓋（tautological）。\n· 規模被低估：`itemOutput` 出現於 **12** 個檔、`summoningrituals` 出現於 **17** 個檔；只 `ritual/summoning_rituals.js` 一個檔就有 **29 個 `.altar(` vs 8 個 `.itemOutput(` vs 93 個 `.input(`**。P2 唔係「只補一類窄嘢」咁細，而係一次過出幾十條 edge，chain 隔離係硬需求。\n· 敍事矛盾（非 code bug，但要知）：P1 出 `full ←(transform)← empty`；P2 出 `empty ←(ritual)← full`（且 `goety_ritual.js:142` 又再消耗 full）。兩個 edge 同時出，答案層可能自相矛盾；S3 只驗 full 一邊，冇驗反向敍事一致性。\n\n【(2) 9 條負對照：邊幾條真可證偽】\n\n先講一個方法論硬傷：**S2 寫「9 條全部要紅→綠」，但 ①–⑧ 全部係「斷言零 fact／零 edge」**。P1/P2 parser 今日**根本唔存在**，所以今日跑就已經係「零」＝**綠**，永遠觀察唔到「紅」。要做到「紅→綠」，harness 必須內含一個**故意寬鬆嘅 naive parser**（或刻意先寫寬版）令 negative fixture 真係出 edge，再對真 parser 斷言 0。否則 ①–⑧ 係 vacuous（green-by-absence，唔係 green-by-rejection）。\n· ① `constdef.js:253/255/274` → **tautological＋形狀描述錯**。`machineChestLootTable`（:253）係**陣列** `['kubejs:platelet_dispatcher', …]`，唔係 object literal，連「const 物件字面值」都唔係；`warpFoodMap`（:255）／`tagWorth`（:274）value 係 object/number 而唔係 function → 三條全部被必要條件 1 排除，同一條 regex 就擋死，冇新增辨別力。真代表「同 shape 但唔係變換」嘅係**全包 ~150 個** `'b_a_d:xxx': function(){…}` map（例 `server_scripts/b_a_d/b_a_d_player_damage.js:10`、`b_a_d_key_bind.js:10/78/91…`）。\n· ② `constdef.js:60` 註解內 `twilightforest:yeti_alpha` → **already-green-by-accident**：ITM regex 係 `['\\\"]([a-z0-9_]+:[a-z0-9_./-]+)['\\\"]`（`PackIndex.java:43`），註解內嘅 quoted id **今日已經會被 `indexScriptItems`/`indexAcquireFile` 收進索引**（冇 comment stripping）；而 P1 條件 1 本身要求 const object + function value，該註解位於一個**陣列**內 → 又係被條件 1 排除。要真有意義，fixture 應該係「const map 內部、function body 內嘅註解行寫住第二個 id」。\n· ③ `${…}` 動態 id → **tautological**（`ITEM` regex 要求 `[a-z0-9_]+:` 緊貼引號，`` `…` ``／`${` 一定唔 match）。真例子應該用 `Item.of(<變數>)`：`b_a_d_player_damage.js:1501 Item.of(mainitem.id)`、`b_a_d_key_bind.js:106 Item.of(item.id)`、`b_a_d_organ_active.js:266 Item.of(itemId)`。\n· ④ `Item.of('')` → **tautological**（空字串唔可能 match `ns:path`）。\n· ⑤ `Item.of('id','{NBT}')` 第 2 arg → **tautological**：條件 2 本身已寫明「第 2 個 arg 係 NBT/count → 唔理」，即「只讀 arg1」，同必要條件同一句話。**但佢有個真危險孿生兄弟**：`goety_ritual.js:142` `Item.of('gateways:gate_pearl', '{gateway:\"kubejs:b_a_d/apocalypse\"}')` —— NBT 字串內嘅 `kubejs:b_a_d/apocalypse` **完全符合** ITEM regex（`/` 在允許集內），任何「連 NBT 一齊掃」嘅實作都會出假 edge。⑤ 應該改用呢個真 fixture，而唔係泛泛嘅 `'{NBT}'`。\n· ⑥ `WeaponInfusionRecipe` arg0／`GoetyRitualRecipe` arg2 → **已經係現況行為，唔會紅**。現有 parser 只認 `SHAPELESS`/`SHAPED`/`LootJS` 等，`new WeaponInfusionRecipe(...)`（`b_a_d/recipe/weapon_infusion.js:14`）、`new GoetyRitualRecipe(...)`（`b_a_d/recipe/goety_ritual.js:1/142`）今日出零 edge；而 P1/P2 條件又要求 const map＋function 或鏈式 builder → 構造子呼叫唔會被命中。即 ⑥ 今日已綠、加完仍綠，屬「保護性回歸」而唔係可證偽測試（同樣要 naive parser 先見紅）。\n· ⑦ `event.addItem`／`tooltip.*` → **同現有實作直接衝突，寫「零 fact」會永遠紅／寫錯**。`PackIndex.java:81-84 INTERACT_GIVE`（`(?:\\\\.give|giveInHand|addItem|spawnAllLocation…)\\\\s*\\\\(\\\\s*(?:Item\\\\.of\\\\()?['\\\"](id)['\\\"]`）＋ `:2124` 使用處 → `event.addItem('kubejs:x')` **今日已經**會出取得 edge（`interactGetGets` 走 right_click/on 路徑）；`PackIndex.java:88-92 INTERACT_DYNAMIC_DROP` 亦專門處理動態 drop。`tooltip` 亦有現行 fact（真 trace `ask-20260915-230340-….jsonl:28` 見到 `-[use]-> 觸發:ItemEvents.tooltip`，來源 `client_scripts/item_tooltips.js:2`）。所以 ⑦ 一定要**收窄為「零 transform／零 ritual edge」**，唔可以寫「零 fact」。\n· ⑧ `.id('ns:path')` → **被條件自動涵蓋（tautological）**，因為 P2 白名單只認 `.altar(`/`.input(`/`.itemOutput(`，`.id(` 根本唔入 parser 視野。真值在於「chain 掃描唔可以用『整條 chain 所有 quoted id』」。\n· ⑨ 「剔除角色表一行 → 閘必須紅」 → **最弱、且係測試測試自己**。佢只能證明 harness 係 table-driven（例如 assert `rows==3`），證明唔到 Java 真係實作咗該角色。真 mutation test 應該係「刪 `PackIndex` 嘅 `.altar(` 分支 → 某條具名 test 必須紅」。\n\n**缺失負對照（附真例）——至少 5 條，全部有真檔證據：**\n(a) **chain 隔離**（最重要）：`kubejs/server_scripts/ritual/summoning_rituals.js` 同檔 **29 `.altar(` vs 8 `.itemOutput(`**，一個 file-wide「同檔同時見到兩者」嘅實作會造出 ~8×29 假配對。fixture：altar 在 chain A、itemOutput 在 chain B（:12、:439 兩條鏈）→ 只准 :439 嘅 chain 出 edge。\n(b) **實體 id 唔准變 item edge**：同檔 `.sacrifice(` 7 次、`.mobOutput(` 21 次，值係 `minecraft:cow` 之類，**符合 ITEM regex**；例如同檔 `.mobOutput('…')`／`.sacrifice('…')` 必須零 item edge。9 條完全冇覆蓋。\n(c) **NBT 內 ns:path**：`goety_ritual.js:142` → 只准出 `gateways:gate_pearl`，唔准出 `kubejs:b_a_d/apocalypse`。\n(d) **`new ItemStack(key,count)` 形狀**：`kubejs/startup_scripts/dlc/dlc_common.js:61/67 inv.setStackInSlot(blanks[i++], new ItemStack(key,max))` —— 接收者＋setStackInSlot＋同檔有 id-keyed map 上下文，係 P1 最接近嘅 false positive 候選，9 條冇一條測「接收者係什麼都唔緊要但 arg 唔係 `Item.of('literal')`」。\n(e) **計數前綴形式**：`summoning_rituals.js` 有 `.input('16x createaddition:gold_spool')`（`'16x '` 令 ITEM regex 唔 match）→ 用嚟證明「消耗側冇出 edge」係因為辨識失敗而唔係方向判定正確；亦測 `Item.of('id', 4)` 計數形式。\n\n【(3) 出到現有 acquire edge 形式係唔係足夠？—— 唔夠，必須改 PackIndex 以外嘅 code】\n\n**§2 嘅核心論斷（「只要入到 acquireFactsDetailed 嘅 bundle，acqShot→HonestMiss→顯示全部自動帶到；Phase 2 唔需要」）係錯嘅**，我逐條 code path 走過：\n1. `PackIndex.java:1265` 嘅 rank 迴圈只有 fish / loot / trade / quest_submit / quest_obtain / recipe_needs / removed / right_click* / right_click_use / right_click_as_block 分支，**冇 trailing else**。`item:<id> -[transform]-> …` 落到尾就靜靜雞被丟，**永遠入唔到 `ranked`**。→ `:1385-1387` 若 `ranked.isEmpty()` 會直接 `AcquireFacts.empty()`。\n2. 於是 `AskEngine.java:346` 嘅 `acqShot` 由 `acquireBundle.lines()`（`AskEngine:287-288`）砌 → **空**；`AskEngine:449-462 acquireLines` 跟住空 → 唔會傳去 LLM。\n3. 唯一看似兜底嘅 `AskEngine.java:472-504` graphLines 迴圈，條件係 `isPurposeGraphFact(gf)` 或 `gf.contains(\"-[drops]->\")||\"-[loot]->\"||\"-[fish]->\"||\"-[trade]->\"||\"-[reward_stack]->\"||\"-[reward_loot]->\"||\"-[removed]->\"` 或 `-[recipe_needs]->`（`PackIndex`/`AskPurposeContext` 已核）—— **`-[transform]->`／`-[ritual]->` 一樣係 fall-through，被丟**。（`selectRelatedGraphFacts`（`PackIndex.java:1086`）冇 label 過濾，所以 edge 其實**會**經 retrieve 嘅 `ingestGraph`（`PackIndex:545`）入到 `retrieved.graphFacts()`；係喺 AskEngine 呢層被丟，唔係冇入到。）\n4. HonestMiss 反而係「反向傷害」：`HonestMiss.shouldPinAcquireMiss` 只在 `acquire` **空**時 pin 未知行；真 trace（`ask-20260915-230340-….jsonl`）今日就係答「取得方式未知」。若新 edge 入唔到 `lines()`，S3 要求嘅「由神恩項鍊（空）於擊敗 boss 後轉換取得」**永遠唔會出**，而答案仍然係「未知」——即 S3 一定 FAIL。\n5. 另外兩重封頂：`PackIndex.java:28 MAX_GRAPH=200` ＋ `addFact`（`:2541`）在滿額時**靜默丟棄**；`AskToolContext.java:31/33` `MAX_ACQUIRE_LINES_FULL=12`、`SLIM=3`，而 `:143 clipAcquireLines` 用 `clipLines` 只保留**頭 N 行**、`PackIndex:1390` 恆例插一行 header → 非「怎麼取得」問句只剩 **2 條真行**。plan 冇為 transform/ritual 定 band，新 edge 隨時被 clip 走。\n6. 仲有 reachability：`acquirePathsByItem` 只由 `indexAcquireFile`（`:1542`，只在 `isAcquirePath(pl)` 即 fishing/loot/trade/quest 時才叫，`:233-235`）＋ `pinLootJsAcquirePath`（`:333`）＋ `pinLootContainsAcquirePath`（`:359`）填；`indexScriptItems`（`:1568`）**只填 `inverted`，唔填 `acquirePathsByItem`**。`kubejs/server_scripts/curios/entity_death.js` 唔係 acquire path → **唔會**出現在 `acquireFactsDetailed:1224` 嘅 `rels`。所以旗艦個案連「被 parse」都未必發生（只能靠 retrieve 嘅候選路徑 `:510 read>=40`、`:538 bodyMentionsSeed` 僥倖命中）。\n7. `ReplyLang.kindLabel`（`:1181-1203`）**唔係顯示層**：我只搵到 `PackIndex.java:2249 String label = ReplyLang.humanAcquireLabel(\"zh_tw\", rel, kind);` 一個呼叫點，即 `kindLabel` 係 **ingest 期砌 label 字串**用。顯示層嘅 fallback 係 `AskEngine.formatInteractOrAcquireFact` 末尾 `Plainify.humanizeGraphFact`，而佢**根本到唔到**（第 3 點）。§2 用「kindLabel 有 fallback」推論「唔需要改 answer layer」係 category error。\n結論：**需要改 PackIndex 以外嘅 code（AskEngine），亦需要改 PackIndex 內 position 排序與路徑索引。**\n\n【(4) 具體必要修改】見 required_changes。另註：§3「另加」嘅 peer bug **係真嘅、我有 trace 實證**（`packai/trace/ask-20260915-230340-kubejs_god_bless_full_necklace.jsonl:28` 出咗 `同 tag 其他成員: kubejs/client_scripts/item_tooltips.js:2（無官方名）;`），但 plan 建議嘅修法位置錯（見 required_changes 最後一項）。",
  "required_changes": [
    "PackIndex.java:1265-1379（rank 迴圈，`for (String f : graphFacts)`）——必須新增 `f.startsWith(prefix + \" -[transform]-> \")` 與 `-[ritual]->` 兩個分支，否則新 edge 無 else 兜底、永久被丟，`acquireBundle.lines()` 為空、`AskEngine:346 acqShot` 為空、S3 必 FAIL。順手更新 PackIndex.java:1197-1200 嘅 AcquireFacts javadoc（現時只列 fish|loot|trade|removed）。",
    "PackIndex.java:1388 附近——為 transform／ritual 指定 RankedAcquire band（建議 band 2，與 interact 同級、高於 generic loot band 1／script band 4），令佢在 `AskToolContext.java:33 MAX_ACQUIRE_LINES_SLIM=3`（`PackIndex.java:1390` header 恆佔 1 行 → 實際只剩 2 行）之下仍有機會出線；並在 band 註解（PackIndex.java:1398）補上新角色。",
    "AskEngine.java:477-492（graphLines 過濾）——加 `gf.contains(\"-[transform]->\") || gf.contains(\"-[ritual]->\")`（若要 overflow 也帶出），否則 `retrieved.graphFacts()` 內嘅新 edge 在此被 fall-through 丟棄；若要走 PURPOSE 通道，另在 AskPurposeContext.java 嘅 `isPurposeGraphFact` 加同一對 label。",
    "PackIndex.java:1685-1696（`ingestGraph` 內 `isScriptPath(pl)` 段）——新增**獨立**函式 `ingestTransformEdges(rel, text)` 與 `ingestRitualEdges(rel, text)`；**嚴禁**照 v4 §5 字面重用 `ingestAcquireEdges(rel, text, \"transform\")`，因後者（PackIndex.java:2248-2260）用 `ITEM` regex 掃成個檔、對每個 id 都出同一 label，會為被消耗嘅 key 出反方向取得 edge（entity_death.js 會同時出 empty 與 full；entity_hurt.js 會令 kubejs:friend_to_the_end 變可取得）。",
    "PackIndex.java:1568-1600（`indexScriptItems`）或 :233-237（buildIndex 分派）——令含 P1/P2 pattern 嘅腳本檔入 `acquirePathsByItem[outId]`（比照 :352／:1555／pinLootJsAcquirePath），否則 `acquireFactsDetailed:1224` 嘅 rels 永遠唔含 `kubejs/server_scripts/curios/entity_death.js`；同時要處理 `:1232` 10-rel 上限與 retrieve `:510` read>=40 上限對旗艦個案嘅截斷。",
    "PackIndex.java:28（MAX_GRAPH=200）＋ :2541-2546（`addFact` 滿額靜默丟）——transform／ritual edge 要用 `addFactForced`（:2549）或先於其他 ingest 執行，否則在大型整合包會靜默消失而 harness 唔會發現。",
    "docs/plans/2026-09-16_kubejs-transform-narrow-v4.md:37——P2 pattern sketch 由 `.itemOutput(Item.of('out'))` 改為 `.itemOutput('kubejs:out')`：真旗艦 `kubejs/server_scripts/ritual/summoning_rituals.js:439` 係裸字串，該檔 8/8 個 itemOutput 全部裸字串、全檔只出現 1 次 `Item.of`；照現 sketch 實作 S1「P2 出到 -[ritual]->」必 FAIL。",
    "docs/plans/2026-09-16_kubejs-transform-narrow-v4.md:12——刪除「Phase 2（改 answer layer 決策）唔需要」及「顯示層對未認識 label 有 fallback：ReplyLang.kindLabel」兩句：`ReplyLang.kindLabel`（ReplyLang.java:1181-1203）唯一呼叫點係 `PackIndex.java:2249`（ingest 期砌 label），唔係顯示期；實際顯示 fallback `AskEngine.formatInteractOrAcquireFact` 结尾 `Plainify.humanizeGraphFact` 因 PackIndex.java:1265 與 AskEngine.java:477 兩個 fall-through 永遠達唔到。",
    "docs/plans/2026-09-16_kubejs-transform-narrow-v4.md:56（S2 ①–⑧）——改寫為兩類：(A) 真值閘（fixture 直接餵 parser，斷言 0 edge）；(B) 反例紅→綠**必須**先在 harness 內提供一個故意寬鬆嘅 naive parser（例：`Item\\.of\\(\\s*'([^']*)'` ＋ 整條 chain 掃全部 quoted id），令 fixture 真係出 edge（見紅），再對真 parser 斷言 0；否則 P1/P2 parser 今日唔存在 → ①–⑧ 今日已係綠，「紅→綠」不可觀察（green-by-absence）。",
    "docs/plans/2026-09-16_kubejs-transform-narrow-v4.md:56（S2 ⑦）——由「`event.addItem`／`tooltip.*`／`scene.world.setBlock` → 零 fact」收窄為「零 transform／零 ritual edge」：`PackIndex.java:81-84 INTERACT_GIVE` 已在 `:2124` 為 `event.addItem('id')` 出取得 fact（`PackIndex.java:88-92 INTERACT_DYNAMIC_DROP` 亦處理動態 drop），`tooltip` 亦有現行 fact（真 trace `ask-20260915-230340-….jsonl:28`）；寫「零 fact」會永久紅。",
    "docs/plans/2026-09-16_kubejs-transform-narrow-v4.md:56——新增 5 條缺失負對照（全部有真檔）：(a) chain 隔離——`kubejs/server_scripts/ritual/summoning_rituals.js` 同檔 29 個 `.altar(` vs 8 個 `.itemOutput(`，file-wide 實作會造 ~8×29 假配對（:12 與 :439 分屬兩鏈）；(b) 實體 id——同檔 `.sacrifice(` 7 次／`.mobOutput(` 21 次，值符合 ITEM regex，必須零 item edge；(c) NBT 內 ns:path——`kubejs/server_scripts/b_a_d/recipe/goety_ritual.js:142` 只准出 `gateways:gate_pearl`、唔准出 `kubejs:b_a_d/apocalypse`；(d) `new ItemStack(key,max)`——`kubejs/startup_scripts/dlc/dlc_common.js:61/67`；(e) 動態／計數形式——`Item.of(mainitem.id)`（`b_a_d_player_damage.js:1501`）、`Item.of(item.id)`（`b_a_d_key_bind.js:106`）、`Item.of(itemId)`（`b_a_d_organ_active.js:266`）、`.input('16x createaddition:gold_spool')`。",
    "docs/plans/2026-09-16_kubejs-transform-narrow-v4.md:56（S2 ①）——改為使用真「同 shape 但唔係變換」fixture：`kubejs/server_scripts/b_a_d/b_a_d_player_damage.js:10`、`b_a_d_key_bind.js:10/78/91`、`b_a_d_food_eaten.js:10/33` 等約 150 個 `'ns:id': function(event, organ){…}` map。另註 `constdef.js:253 machineChestLootTable` 係**陣列**（非 object literal），形狀描述需更正。",
    "docs/plans/2026-09-16_kubejs-transform-narrow-v4.md:56（S2 ⑨）——改為真 mutation test：刪除 PackIndex 內 `.altar(`（或 `Item.of('literal')`）分支 → 指定具名 test 必須轉紅；現寫法只證明 harness 係 table-driven（測自己）。",
    "OfficialDisplay.java:216（`annotatable()` 內 `if (path.contains(\".js\") || path.contains(\".snbt\") || path.contains(\".json\"))`）——§3「另加」嘅 peer bug 是真嘅（trace `packai/trace/ask-20260915-230340-kubejs_god_bless_full_necklace.jsonl:28` 實際出咗 `同 tag 其他成員: kubejs/client_scripts/item_tooltips.js:2（無官方名）;`），但**修法位置錯**：`resolveAnnotatable`（OfficialDisplay.java:166-186）把 hit 按 ':' 切段後只把**最後一段**當 `path`（此例 = \"26\"），所以 `.js` 檢查永遠唔會命中，而 ns 段 `kubejs/client_scripts/item_tooltips.js` 通過了 `annotatableNamespace`（只拒 item/source/tier/note/file）。應在 `resolveAnnotatable`/`annotatable` 對 **ns 段**加 `.js`／`.json`／`.snbt`／`/` 拒絕，而唔係只在 `collectPeers`（OfficialDisplay.java:144-159）剔 `evidence=`／`source:` 尾段（後者只治單一 fact 形態）。"
  ]
}
```