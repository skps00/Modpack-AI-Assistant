```json
{
  "ratio": "正方 6 : 反方 4",
  "verdict": "【結論】方向正確（窄範圍、單檔 predicate、harness 先行、紅先於綠），P1–P5 對『已觀測垃圾』實測係完備嘅，但有 3 處硬傷令獨立 verifier 做唔落去：斷言自相矛盾（永遠綠唔到）、閘現況 RC 寫錯兼 scope 做唔到、baseline『115 check 全綠』未驗證。必修訂後才開工。\n\n(1) P1–P5 完備性／一致性（已用真 trace 逐字驗證）。我用 Python 重寫舊／新 ITEM_ID regex ＋ resolveAnnotatable ＋ annotatable ＋ collectPeers，跑 39 個 ask-*.jsonl 全部 fact 行。舊 predicate annotate 21 個 id；加 P1–P4 後 21→14，丟棄嘅 7 個全部係垃圾（count:1b、kubejs/client_scripts/golden_age/item_tooltips.js:1、kubejs/client_scripts/item_tooltips.js:2、kubejs/server_scripts/common/player_login.js:2、kubejs/server_scripts/mrqx_extra_pack/mrqx_common/mrqx_events.js:518、kubejs/client_scripts/strayexpansion/straytooltips.js:1、mechanic:none），新增 0 個；14 個合法 id（eccentrictome:tome、golden_age:infinity_sword(_organ)、golden_age:donut、mrqx_extra_pack:page_of_future／page_of_past、kubejs:colorful_candy、kubejs:god_bless_full_necklace、stray_expansion:chestopener_*）全數保留，冇誤殺。多段合法 id 亦逐個驗過唔會死（ino_dlc_build:music.build_henshin、mrqx_extra_pack:item/mystery_item、ino_dlc_build:item/item/build_phone）。NBT `{Count:1b,id:...}` 係 P3 殺（ns=Count 含大寫），唔係 P4 —— resolveAnnotatable 已 toLowerCase，加上 P3 唔准大寫 ns，所以 P4 名單內嘅 Count／Damage／id／tag 大寫項係死碼；真正 load-bearing 嘅只有小寫 fact label（mechanic／via／src…）。\n\n(2) 反例／殘留缺口（兩類）。(a) P5 只守 peer 行，annotate()（:87-109）冇對應『必須有官方名』規則：任何過得 P1–P4 但解唔到 hover 名嘅 id，一旦落入 fact 本體就會照出（無官方名），而 §4 閘只查 peer 行 → 靜默過關。我已驗證呢類 id 確實存在於本機 trace 文本（additional_attributes:spell_general、minecraft:generic.attack_damage、forge:reach_distance、irons_spellbooks:summon_damage 全部會被新 predicate resolve 成合法 id）。今日 19 條真 fact 行冇呢個殘留（所以無現成反例），但閘要覆蓋 fact 本體先叫完備。(b) P1 嘅 lookbehind 含減號，緊接減號嘅合法 id 唔再 annotate；39 條 trace 未見實例，屬已知 false negative，要寫入 §5 最壞情況。\n\n(3) 紅先於綠：fixture = AskDisplayNameCheck.java:55-72 嗰條 fact 字串；命令 = 先 `python research/gen_tmp_check.py`，再喺 forge/1.19.2 跑 `./gradlew.bat -I tmp-check.gradle runAskDisplayNameCheck`（gen_tmp_check.py 與 forge/1.19.2/tmp-check.gradle 都真實存在，命令係真嘅）。現有 2 段 fixture 真係靠意外通過：`(source:ftbquests/quests/demo.snbt tier:C)` 兩個 token 切段後 ns 分別係 source／tier，全部命中 annotatableNamespace 黑名單 → resolveAnnotatable 回 null；而且整條 fixture 冇任何 3 段 token，即係從來冇行過『中段被當 ns』嘅 bug 路徑（我用 predicate 模擬確認）。但有硬傷：§4 要求斷言「peer/fact 內不得出現 js:／.js」係自相矛盾 —— 修好之後 raw 腳本路徑（…item_tooltips.js:2）按 §3『唔改 fact 內容格式』仍會原文留喺 fact 本體（P2/P3 只係令佢唔被 annotate，唔會刪字），所以 body 永遠含 .js／js:，紅→綠永遠走唔完。斷言必須分層：peer 行（enrichFacts 回傳中 PEER_HEADER 開頭嗰個元素）禁 .js／js:／（無官方名）；fact 本體只禁（無官方名）。另：修前 RED 必須係獨立一步（先加 fixture 落 commit／先跑攞 FAIL 輸出），否則 verifier 重現唔到紅。\n\n(4) 閘與版本 scoping。我實測 `python tests/check_ask_display_leak.py` → RC=2（NO LOG LINES；`grep -c 'Pack AI display body ver=' latest.log` = 0），所以 plan §4『現時該閘 RC=0』係錯；而 repo AGENTS.md:63 嘅 runner（`for f in tests/check_*.py; do python ... || echo FAIL`）任何非 0 都當 FAIL → 呢個閘目前係紅，plan『現有 115 check 全綠』（AGENTS.md:77 自己寫 110/110）未驗證兼數字唔一致。另外現有閘只有 display-body 模式（parse_display_lines 只認 ver= 行，仲只取 parsed[-5:]），而 trace event 冇 buildId（keys 只有 event/ts/name/round/result）→ plan『按 build scope』根本實作唔到；要改成 `--trace <instance>/packai/trace` ＋ 用檔名 ask-YYYYMMDD-HHMMSS-*.jsonl 做日期 scope，斷言對象係 result 字串中以『同 tag 其他成員:』開頭嘅行。\n\n(5) 過度宣稱：大致 OK —— plan 認咗 OfficialDisplay 係純函式、無 cache／schema 改動（我核對 enrichFacts 只喺 AskService.java:775 即時呼叫，mechanic-cache 存 raw 掃描，唔會留住已 annotate 字串），亦冇宣稱跑過驗收。但『實測 odd namespace = 0／4,321 檔』核唔到（我實測 instance/minecraft/kubejs = 9144 檔、648 個 .js，plan 未畀命令／路徑）；plan 第 6 行嘅類別計數（24／18／3）同我真 trace 掃描完全吻合，但總數 67／64 我用唔同切法得 54（51 垃圾＋3 合法）＋16 個人化重複（整合包脚本（無官方名）／none（無官方名）），即係 plan 要補上『點數出嚟』嘅命令先可被審。\n\n【未能驗證】① 冇跑 gradle，紅／綠只靠 code 級 predicate 模擬，非執行證據；② 未跑真機煙霧測試（無法開 MC）；③ 『115 check 全綠』與『4,321 檔』兩個數字；④ P4 黑名單會否撞到本 pack 真實 ns（entity／dimension／table／from 等）——冇證據但理論可能，要列為接受風險 ＋ harness 反例；⑤ 67／64 精確總數。",
  "required_changes": [
    "AskDisplayNameCheck.java:55-75（peerSectionAndNoInvented）：斷言分層 —— peer 行（enrichFacts 回傳內以 OfficialDisplay.PEER_HEADER 開頭嗰個元素）斷言 !contains(\".js\") 且 !contains(\"js:\") 且 !contains(OfficialDisplay.NO_OFFICIAL)；fact 本體只斷言 !contains(NO_OFFICIAL)（唔可以對 body 禁 .js／js:，因為修好後 raw 腳本路徑按 §3 仍原文留低，該斷言永遠綠唔到）。同時加紅 fixture：`item:…（focus） -[use]-> … (source:kubejs/client_scripts/item_tooltips.js:2 tier:A)` 同 `mechanic:none` 兩行，並保留彩虹糖果類正向對照（stub lookup 要有名，否則 P5 會連正向對照一齊吞）。",
    "docs/plans/2026-09-16_display-peer-leak-fix.md:29-31（§4 紅證據）：寫實命令與次序 —— 先只加 fixture 跑 `python research/gen_tmp_check.py` 然後（cwd = forge/1.19.2）`./gradlew.bat -I tmp-check.gradle runAskDisplayNameCheck` 攞 FAIL 輸出存證，再改 predicate 跑同一命令攞 PASS；並把 fixture 加入與 predicate 改動分成兩步，令 verifier 可獨立重現紅。",
    "docs/plans/2026-09-16_display-peer-leak-fix.md:33（§4 閘）＋ tests/check_ask_display_leak.py:38-53：刪『現時該閘 RC=0』（實測 RC=2，latest.log 內 ver= 行數 0），改為記錄實測 RC 與 AGENTS.md:63 runner 下嘅紅／綠；新增 trace 模式（`--trace <instance>/packai/trace`、斷言『同 tag 其他成員:』行）與日期 scope（檔名 ask-YYYYMMDD-*，唔可以用 build scope——trace event 冇 buildId），並加 fact 本體禁（無官方名）規則以覆蓋 P5 守唔到嘅殘留。",
    "OfficialDisplay.java:190-200（P4）＋:202-225（P2/P3）＋:166-188（resolveAnnotatable）：明寫 P4 只作用於 ns 且只列小寫（大寫 Count／Damage／id／tag 係死碼，因 resolveAnnotatable 已 lowercase 且 P3 禁大寫 ns），P2 施加於整個 resolved id 全部段，P3 要求 ns 為小寫無斜線；harness 要加反例 js:2 家族、`{Count:1b,id:…}`、3 段 `source:kubejs/…js:2`、同 2 段 `mechanic:none`，以及正向多段 id（ino_dlc_build:music.build_henshin、mrqx_extra_pack:item/mystery_item、ino_dlc_build:item/item/build_phone）唔准被殺。",
    "OfficialDisplay.java:144-159（collectPeers）＋docs/plans/…:36-38（§5）：加 P5（peer 必須 officialName(id) 非空）時明寫佢只護 peer 行、fact 本體殘留（無官方名）唔在此規則覆蓋（要另加 body 規則或明列接受），並把 P5 會靜默吞掉『合法但唔係 ItemStack 可解』嘅 peer（例如非物品 registry id）列為已接受 false negative；同時補『4,321 檔／odd namespace=0』嘅可重跑命令或刪該數字（我實測 kubejs 9144 檔、648 .js，對唔上）。"
  ]
}
```