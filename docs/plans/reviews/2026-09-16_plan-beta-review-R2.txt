```json
{
  "ratio": "正方 5 : 反方 5",
  "verdict": "v4.1 已經修好 R1 最核心嘅「方向反轉」問題，但 S3 嘅核心斷言（trace 含 raw `-[transform]->`）以 plan 自己揀嘅通道嚟講做唔到，加上 §2.4 pin 錯函式、band 排序講反、addFactForced 會洪泛、漏 MAX_RETRIEVE_FACTS=24，所以未達 8:2，建議第 3 輪只做本地修正（唔改核心設計）。（1）方向：正確。已核真檔——`entity_death.js:22` key=`kubejs:god_bless_empty_necklace`、`:26` `setStackInSlot(slot, Item.of('kubejs:god_bless_full_necklace'))` → `item:full -[transform]-> from:empty` 方向對；`entity_hurt.js:31` key=`kubejs:friend_to_the_end`、`:36` out=`irons_spellbooks:silver_ring`，所以「問普通物品 → edge 0」用 silver_ring 係錯嘅（plan 已自行修正 ✓）；§1 禁用 `ingestAcquireEdges`（`PackIndex.java:2248-2260` 用 ITEM regex 掃全檔、`:2249` 用 ingest 期 humanAcquireLabel）係正確診斷。全 pack `setStackInSlot(...Item.of(` 實測只有 2 處、`Item.of(` 喺 `summoning_rituals.js` 只 1 次（:8，而且係被消耗嘅 `.input(Item.of('minecraft:potion',{Potion:\"minecraft:strong_turtle_master\"}))`）。鏈隔離：技術上做得到，因為每個儀式係一條 `;` 結尾嘅 statement（`summoning_rituals.js:16/29/94/111/431/439` 都係 `.recipeTime(n);`），29 條鏈 ＝ 29 個 `.altar(` ＝ 8 `.itemOutput(` ＋ 21 `.mobOutput(`，所以鏈內配對只會出 8 條；但要留意 `:100-107` `.mobOutput( … )` 跨行、後面空行再接 `.input(` → splitter 要括號感知，唔可以純行切。而且「唔會 8×29 假配對」其實係靠「只 emit `.itemOutput` 參數」呢條規則，唔係靠鏈隔離；鏈隔離真正負責嘅係 CONSUMES 分類（同行 `.altar(X)`／`.input(...)`）同渲染上下文。另外 `:8` 嘅 `Potion:\"minecraft:strong_turtle_master\"`（引號內 ns:path）同 `:437` `.blockBelow('minecraft:command_block')`、`:6/436` `.id('kubejs:ritual_*')`（29 次 recipe id）全部都會中 ITEM regex，§1 丟棄清單只提 goety `:142` 嘅 NBT，漏咗呢批。（2）§2 清單唔齊而且通道混淆：rank 迴圈（`:1265-1379`，現時無 else、的確永久 fall-through）、graphLines（`AskEngine.java:472-504`）、purpose 白名單（`AskPurposeContext.java:48-61`）、`MAX_GRAPH=200`（`:28/:2541-2546`）、retrieve 40 檔（`:510`）、10-rel（`:1232`）、toggle（`PackAiConfig.java:152/:439`）、cache（`KubeJsMechanicScan.java:1093`）都有覆蓋；但 (i) §2.4 指 `indexScriptItems`（`:1568-1600`）去令檔入 `acquirePathsByItem` 係錯——該函式只寫 `inverted`（`:1581-1586`），而 `:233-238` 分派令 `kubejs/**` 只入 `scriptRels`（`isAcquirePath` 只認 fishing/loot/trade/quest，`:2575-2580`），正確 hook 係 `ingestItemDescriptions`（`:302-330`）照 `pinLootJsAcquirePath`（`:333-356`）pin；(ii) 漏 `MAX_RETRIEVE_FACTS=24`（`PackIndex.java:35/:563/:1086-1117`，`selectRelatedGraphFacts` 先食 `descByItem` 再截，overflow 新 edge 會靜默消失）；(iii) `addFactForced`（`:2549`）如果放喺 `ingestGraph` 通用路徑，retrieve 讀 40 檔時每個 script 嘅新 edge 都會繞過 200 上限 → 洪泛，應照 `pinFocusLootJsFacts`（`:2275-2291`）只 pin `item:<focusId>`；(iv) band 理由講反——`:1398` 註解 fish0→loot1→interact2→trade3→script4，`:1388` 按 band 升序，band 2 係**低**於 loot band 1，而 obtain 問題真正 binding 係 `MAX_ACQUIRE_LINES_FULL=12`（`AskToolContext.java:31/:118-120`，12 已在 `PackIndex.java:1266` 封頂），唔係 SLIM=3；(v) 通道混淆：`KubeJsMechanicScan` cache/schema 只對 mechanic fact 通道有意義，但 §1 嘅新函式係 PackIndex 通道 → 兩者只可揀一個；(vi) §2 冇 forward 期 `INDEX_SCHEMA_VERSION`（`:96/:505/:531`）決定；(vii) python 鏡像 `tests/check_kubejs_universal_scan.py` 要寫明跟唔跟。(3) S1/S2/S4 大致可證偽（S2 真值閘＋naive parser 見紅＋mutation＋真檔 5 條負對照都係好設計；`tests/check_kubejs_universal_scan.py:129-137/160/174` 行號引用正確；`tests/check_*.py` 實測 115 個，plan 寫 115 ✓），但 S3 唔可證偽：raw marker 走 acquire/graphLines 通道時會被 `AskEngine.java:1389` → `Plainify.java:218`（`replace(\"-[ ->  →\").replace(\"]->\",\" \")`）消滅，永遠唔會出現喺 `send.facts`（`AskTrace.java:749`）；真 trace 證明只有 mechanic 通道會帶 raw marker（`item:满溢神恩项链（kubejs:god_bless_full_necklace） -[use]-> 觸發:ItemEvents.tooltip …`）。§4 還原大致齊（jar 由 `%TEMP%\\deploy_backup_*`／`mc_mod_deploy_jar.py`＋sha256、schema／version scope、revert 後舊 code 上 S4 必紅），只欠「forward 有冇 bump schema」嘅對稱決定；(4) 具體 required_changes 見下。",
  "required_changes": [
    "寫死「事實通道」二揀一（現時 §1 用 PackIndex、§2.7 用 KubeJsMechanicScan）：`KubeJsMechanicScan.java:1019-1022` 已出 raw `item:… -[use]-> 觸發:…（source:kubejs/…:N tier:A）`，經 `AskService.java:759-762`（`factsForItem`）→ card behavior → 真 trace 已見 raw marker（`packai/trace/ask-20260915-230340-kubejs_god_bless_full_necklace.jsonl`）；PackIndex acquire 通道則只出已渲染文字。S3 要驗 raw `-[transform]->`，就必須走 mechanic 通道或另加 raw facts block。",
    "S3 斷言修正（`docs/plans/2026-09-16_kubejs-transform-narrow-v4.1.md:36`）：raw marker 經 acquire/graphLines 通道會被 `AskEngine.java:1389` → `Plainify.java:218` 消滅（`replace(\"-[ →  → \")`），所以現寫法唔可能 PASS。二揀一：① 走 mechanic 通道（raw 入 `send.facts`，`AskTrace.java:749`）② 改為斷言新 ReplyLang 渲染文字＋明確放棄「raw marker 可驗」。",
    "§2.4（plan:24）hook 改正：`PackIndex.java:1568-1600 indexScriptItems` 只寫 `inverted`（:1581-1586），唔寫 `acquirePathsByItem`；且 `:233-238` 分派令 `kubejs/**` 只入 `scriptRels`（`isAcquirePath` 只認 fishing/loot/trade/quest，`:2575-2580`）。應改為喺 `ingestItemDescriptions`（`:302-330`）照 `pinLootJsAcquirePath`（`:333-356`）新增 pin，Ask 期再靠 `acquireFactsDetailed:1237 → ingestGraph` re-ingest。",
    "§2.5（plan:25）改正：`ingestGraph`（`:1685-1696`）內用 `addFactForced`（`:2549`）會令 retrieve 讀 40 檔（`:510`）時每個 script 嘅新 edge 都繞過 `MAX_GRAPH=200`（`:28/:2541-2546`）→ 洪泛。應照 `pinFocusLootJsFacts`（`:2275-2291`）做 `pinFocusTransformFacts(itemId,text)`，只 pin `item:<focusId>`，並喺 `PackIndex.java:1238` 旁呼叫。",
    "§2 漏 `MAX_RETRIEVE_FACTS=24`：`PackIndex.java:35`＋`:563`＋`:1086-1117`（`selectRelatedGraphFacts` 先食 `descByItem` 再食 graphFacts，滿 24 即 `return`）→ overflow/graphLines 通道靜默截走新 edge。要嘛加入清單，要嘛明寫「只依賴 ranked 12，唔依賴 overflow」。",
    "§2.2（plan:22）band 修正：`PackIndex.java:1398` 註解 fish0→loot1→interact2→trade3→script4→quest5/6，`Comparator.comparingInt(band)`（`:1388`）＝數字細優先 → band 2 **低於** loot band 1，「高於 loot band 1」係講反。同時 obtain 問題真正 binding 係 `AskToolContext.java:31/:118-120 MAX_ACQUIRE_LINES_FULL=12`（唔係 SLIM=3），而 12 已在 `PackIndex.java:1266` 封頂 → 要嘛改 band 0/1，要嘛 S3 加「新 edge 必須出現喺 ranked 12 條內」斷言。",
    "P2 edge 語法未定義（plan:12-16）：只寫 `-[ritual]->`。要寫死格式（例 `item:<out> -[ritual]-> altar:<altarArgId> src:<rel>:<line>`）＋寫明 `PackIndex.java:1265-1379` rank 分支 parse 邊個 key，否則 S1 冇 deterministic assert 可寫。",
    "S1 量化（plan:30）：要寫「`summoning_rituals.js` 正好 8 條 ritual edge＋8 個 output id 全列」；零 edge 清單要加：`kubejs:god_bless_full_necklace`（`:435 .altar(`）、`minecraft:nether_star`（`:438 .input(`）、`kubejs:ritual_sloth_shard` 等 29 個 `.id(`（`:6/:20/:436…`）、`minecraft:command_block`（`:437 .blockBelow(`）、`minecraft:potion`＋`minecraft:strong_turtle_master`（`:8 Item.of('minecraft:potion',{Potion:\"minecraft:strong_turtle_master\"})`，兩者都會中 ITEM regex）。現 §1 丟棄清單只提 goety `:142`。",
    "旗艦第三真 site 未交代：`kubejs/server_scripts/b_a_d/recipe/goety_ritual.js:142` 真係產出 `Item.of('kubejs:god_bless_full_necklace')`（同埋 `Item.of('gateways:gate_pearl','{gateway:\"kubejs:b_a_d/apocalypse\"}')`）。§1（plan:18）只寫 NBT 要丟 → 要明寫呢個 literal 產出係「本 plan 範圍外（已知 false negative）」定「要 emit」，並加斷言（唔准變反向 edge）。",
    "行號/路徑歧義：`constdef.js` 有兩個——`kubejs/server_scripts/utils/constdef.js`（325 行，`:253` 係 `machineChestLootTable` 陣列）同 `kubejs/startup_scripts/utils/constdef.js`（46 行）。plan:18「`constdef.js:60`」與 plan:31「`constdef.js:253`」要寫全路徑；另 §1/§3 其他檔名（`entity_death.js` 等）統一寫 `kubejs/server_scripts/...`。",
    "S2 數字改準（plan:31-33）：`'ns:id': function` 實測 `b_a_d_player_damage.js` 85＋`b_a_d_key_bind.js` 44＋`b_a_d_food_eaten.js` 8 ＝ 137（唔係「約 150」）；naive parser 要加計數 assert（例 `naive >= 8`、真 parser ＝ 0），否則「見紅」唔可量度；`summoning_rituals.js` 全檔 `Item.of(` 只有 1 次（`:8`，屬被消耗），建議寫入 S2 做硬證。",
    "S3 負對照要指名真 id（plan:37）：用可證嘅 `kubejs:ritual_catalyst`（pack 內 99 次，全係 `.altar(` 參數，檔內 29 次）／`kubejs:ritual_god_bless_summon`（`:436 .id()` recipe id）→ 斷言 `-[ritual]->` 數 ＝ 0；可觀察方式照既有前例 `Pack AI mechanic facts item=… kjs=…`（`AskService.java:770`）加 `Pack AI transform edges item=… n=…`。",
    "§2 補 forward 期 schema/cache 決定：`KubeJsMechanicScan.java:96`（INDEX_SCHEMA_VERSION）＋`:505`（讀 guard）＋`:531`（寫）＋`:1093`（cache key sha256(rel+\\0+src)）＋`:1113`（`kjs-*.json`）。走 mechanic 通道 → forward 必須 bump＋salt；走 PackIndex 通道 → 呢兩項係死改動，應刪，§4 對應亦只需清 cache。",
    "§1/§3 要處理 P2 相對既有 JEI 嘅增量：真 trace 已有 `[card:2] 召唤祭坛 满溢神恩项链+下界之星+神恩项链 -> kubejs:god_bless_empty_necklace role=uses`（JEI 已知召喚祭壇 recipe）→ P2 只值「JEI 冇嘅 ritual」且有重複行風險（`:433-439` 同 :12/…29 條）。plan 名叫 narrow，建議明寫 P2 增量或索性只做 P1。",
    "Python 鏡像決定：`tests/check_kubejs_universal_scan.py`（檔頭 docstring 明寫 mirror `PackIndex #5`）係 Java parser 嘅 Python 鏡像 → §4 新閘要寫明「跟唔跟鏡像、唔跟嘅理由」，否則 115 check baseline 會 drift。",
    "§4 補對稱句＋配套（plan:40-43）：明寫 forward 有冇 bump `INDEX_SCHEMA_VERSION`（冇 bump → rollback 亦只需清 `config/packai/mechanic-cache/`）；因新 line 含 `-[…]->`／`->` 序列，revert 後同時要跑 `AskMarkerIntegrityCheck`（Java harness）＋`tests/check_ask_marker_integrity.py`（AGENTS.md separator 坑）。"
  ]
}
```