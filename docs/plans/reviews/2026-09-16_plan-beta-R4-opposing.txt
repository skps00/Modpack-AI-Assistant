```json
{
  "sinks_generic": {
    "A": "✓ 本 pack 內企得住，但唔可以叫通用。規則（最近外層 map key ＋ setStackInSlot 第二參數 Item.of 字面）實測 100% 對：2 個真 site 都喺 static literal key 嘅 map（server_scripts/curios/entity_death.js:21-27 key='kubejs:god_bless_empty_necklace'、server_scripts/curios/entity_hurt.js:30-36 key='kubejs:friend_to_the_end'），而且 `[expr]: function` 動態 key 全 pack = 0 次、兩個 strategy map 只有嗰兩處定義（grep curiosDeathStrategies/curiosHurtStrategies = 4 hits 全部喺該兩檔）、`Object.assign(` 246 次但無一個合併到有 setStackInSlot 嘅物件（5 個 setStackInSlot 分佈：entity_death.js:26、entity_hurt.js:36 = Item.of 型；maodlc_key_pressed.js:291 變數、dlc_common.js:61/67 `new ItemStack(key,max)` 按規則正確丟棄）→ 方向亦真（`strategies[item.id]` → 換槽 = 轉換，entity_death 條件係殺 BoMD boss）。致命弱點唔喺判斷而喺聲稱：動態 key／spread 情境本 corpus 零實例（spread 4 hits 全係中文文字假命中）→ §5『換 pack 都成立』無證據。另外『只 parse 命中 statement』對 A 亦唔成立：key 喺 :22、hit 喺 :26，key 根本唔喺 hit 嘅 statement 內。",
    "B": "✗ ownerChainId 規則站唔住（真檔實測）。`.itemOutput(` 217 次／12 檔／145 條 `;`-statement：143 條 statement 剛好 1 個 `.id(`，但 **2 個檔用 ASI（省略 `;`）令多條鏈塌成一個 statement**。① server_scripts/dlc/dlc_summon.js：96 行只有 2 個 `;`（:15、:97），lines 17-96 一個 statement 內藏 6 條鏈 = 6 個 `.itemOutput`（:20/:36/:51/:64/:76/:92）＋6 個 `.id(`（:19/:37/:52/:65/:75/:91），而且次序唔一致（:36 `.itemOutput('kubejs:yuanshen')` 之後 :37 才 `.id('kubejs:yuanshen_recipe')`）→ 6 條 edge 全部只能拿到其中一個 id；② server_scripts/mrqx_extra_pack/mrqx_recipes/mrqx_recipes.js：2953 行、**0 個 `;`**、316 條 `event.recipes.` 鏈、86 個 `.id(`、68 個 `.itemOutput(`（首個 :259，對應 `.id(` :245）→ 整個 2953 行檔 = 一個 statement。合計 74/217（34%）occurrence 嘅 `by:` 無唯一解或會配錯（例：yuanshen 鏈被配成 neidan_recipe）。旗艦 server_scripts/ritual/summoning_rituals.js 反而乾淨（29 鏈＝29 `;`＝29 `.id(`＝29 `altar`＝29 `recipeTime`）✓。附帶：`.itemOutput(Item.of(gateway…,nbt))` 3 條（b_a_d/ritual/summoning_rituals.js:89/118/125）塌成同一 `gateways:gate_pearl` 係資訊丟失；`.inputs([...])`（dlc_summon.js:21/38/53/66/77/93）唔係 `.input(`，規則唔會剝 `64x ` 前綴。",
    "C": "✗ 規則錯 43%，連旗艦驗收例子都係反方向。用 pack 自己嘅 JS class 定義做真值（`function XRecipe(p1…) { this.result = … }`，corpus 內 24 個 class 定義；例 server_scripts/b_a_d/recipe/goety_ritual.js:1 `GoetyRitualRecipe(craftType, ingredients, activation_item, output)`、weapon_infusion.js:2 `WeaponInfusionRecipe(base, addition, output)`、mayacraftdlc/recipe.js:1 `DragonForgeRecipe(input, blood, output, dragonType)`、recipes/cooking.js:6 `CookingRecipe(container, ingredients, output)`）：559 個可解析 site 共出 895 條 edge，487 條對（arg 對應 this.result／this.output／this.results），**382 條係把被消耗輸入當取得途徑**——activation_item 183、container 70、input 46、blood 45、reactant 16、base 9、addition 9、usedItem 2、ingredient 2；餘 26 條 class 無定義（CreateAdditionCharging 系、MixingCauldronRecipeWithFluidNBTa、mrqxCataclysmWeaponFusion）連方向都判唔到。即 Sink C 最大類 GoetyRitualRecipe（170 site，佔全部 30%）每一個都連帶噴一條錯 edge。嵌套／多行／陣列形狀無問題（多行 call 287 個、括號感知 splitter 拆得開；`Item.of` 藏喺陣列內者唔會誤出，係漏報唔係誤報），但 `.setOutput(` 全 pack 0 次（唔存在 builder 形），而真正大類係 `event.recipes.create*` 622＋createDeploying 293＋…（見 counterexamples）。"
  },
  "blockers": [
    "B1【載重．否決 §0.2＋§4 S1】Sink C 旗艦驗收斷言方向反轉。真檔：server_scripts/b_a_d/recipe/goety_ritual.js:1 定義 `GoetyRitualRecipe(craftType, ingredients, activation_item, output)`，:142 = `new GoetyRitualRecipe('lich', [12×Ingredient.of…], Item.of('kubejs:god_bless_full_necklace'), Item.of('gateways:gate_pearl','{gateway:\"kubejs:b_a_d/apocalypse\"}'))` → arg2=activation_item=被消耗、arg3=output。三份獨立證據都話 necklace 係輸入：(a) 真 trace ask-20260915-230340-kubejs_god_bless_full_necklace.jsonl 卡 `[card:4] 魔法仪式 · 13 slots · 黑暗祭坛 满溢神恩项链+2 黑暗祭坛+蕴魂红宝石+12 祭坛基座 -> gateways:gate_pearl role=uses`（該 trace 4 張卡全部 `->` 去其他 id，無一張產出呢件物）；(b) 同一 trace 自己嘅規則明文『[RECIPE_CARDS] 只有 role=input（用作材料）≠取得 — 禁止写怎么来』；(c) 同一件物喺 server_scripts/hpdlc_peifang/peifang.js:219 用 `.input(`、lunasdlc/lunasexrecipes.js:997 用 `Ingredient.of(`、旗艦 :435 用 `.altar(` —— 全部輸入側。所以 §4 S1 要嘅 `item:kubejs:god_bless_full_necklace -[recipe]-> by:lich` 係 plan 自己最忌嘅『輸入當取得途徑』；而 v4 §6⑥（R3-supporting 引述）本來寫對咗（arg2 消耗側→零 edge）→ v4.3 係回歸。另：`by:lich` 其實係 craftType 唔係 recipeId（§1 話『第一個字串參數 ＝ recipeId』對呢個 class 唔準）。",
    "B2【載重．Sink B 規格不可實作】§1 定義 `ownerChainId ＝ 同一 statement（接收者運算式到 ';'，括號感知）內 .id()`，但真 corpus 兩個檔用 ASI：dlc_summon.js 96 行只有 2 個 `;`（6 條 itemOutput 鏈同一 statement）、mrqx_recipes.js 2953 行 0 個 `;`（68 條 itemOutput 鏈同一 statement）→ 74/217（34%）occurrence 嘅 by: 無唯一解。要修必須改成『接收者鏈』界定（`event.recipes.<…>` 起始、下一個同縮排 `event.` 或 `;` 結束），唔可以用 `;`；而 §4 S1/S6 只斷言旗艦 8＋第二檔 13，完全無覆蓋呢兩檔（雖然 §1 自己講『.itemOutput 全市掃（12 檔）』）。",
    "B3【載重．§5『支援多 pack』係 marketing，唔係事實】三條 sink 有兩條嘅錨點係『本 pack／單一 mod 慣例』而唔係 JS 構造：(a) `registerCustomRecipe` 喺 instance 全部 mod jar 內 **0 個**（我 zipfile 掃 mods 目錄 class 位元組），而 pack 內有 47 個本地定義 `function registerCustomRecipe(recipeModel) { event.custom(recipeModel) }`（40 檔，另有 maodlcRegisterCustomRecipe／registerCapsidRecipe）→ 換 pack 若寫 `event.custom(new XRecipe(…))` 或 `event.recipes.*`，Sink C 命中 0；(b) `itemOutput` 只喺 `summoningrituals-forge-1.19.2-2.0.7.jar` 內（單一 mod 嘅 KubeJS builder），唔係 KubeJS 通用構造。加上真正大類完全唔覆蓋：`event.recipes.create` 622、`createDeploying` 293、`createPressing` 55、`createCutting` 48、`kubejs` 46、`ars_nouveau` 23、`event.custom(` 77 次／40 檔、`\"result\":` 32 次／6 檔。所以 §5『換 pack、換 mod 都成立』只可以讀成『換 pack 之後規則仍然會跑（多數情況命中 0）』，唔可以讀成『支援多 pack 嘅取得途徑』。",
    "B4【Q4 第二半：『只 parse 命中 statement』係假】Sink C 要分輸出／輸入，必須知 class 定義喺邊個 arg；定義喺檔頭、hit 喺好遠（goety_ritual.js:1 vs :142；mrqx_recipes.js:8/:74/:124 vs hit 到 :2953）→ 只 parse statement 根本無真值（呢啲 class 名硬編碼又唔准）。Sink A 亦唔得：key 喺 entity_death.js:22、hit 喺 :26，key 唔喺 hit 嘅 statement 內。而且 §2 自相矛盾：索引已經存 `byId`（`List<(rel,line,kind,byId)>`）→ ask 期根本唔需要再 parse；若真係只存 (rel,line) 就要全檔 context。正確講法應係『ask 期只讀 index 條目（O(1)），唔 parse 任何檔』。",
    "B5【Q1 回答】§0 兩條決定：blocker 1（S3 raw marker 通道）**實質解到**——改成斷言 render 輸出之後，`Plainify.java:218` 消滅 marker 嘅問題唔再影響驗收；但替補斷言有兩個新弱點：(i) §3.1/§3.3 冇寫死 label 內含 `src:<rel>:<line>`，若唔含，S3 只係 match 一句泛用文字，**分唔到方向對錯**（即 B1 嘅反方向 edge 都會令 S3 綠）；(ii) 負對照 `kubejs:god_bless_summon`（實測 2 次／2 檔、全 pack 無 site）係『斷言一個根本無 edge 嘅 id 有 0 edge』→ trivial PASS。blocker 3（inverted 有損）**解到**：§2 棄 inverted 正確，實測 indexScriptItems 確有 `seen.size() < 120`（PackIndex.java:1576）＋indexAcquireFile 有 `< 80`（:1550），而 mrqx_recipes.js 一檔就有 86 個 `.id(`；新上限（每檔 2,000／全局 50,000）對本 pack 夠（單檔最多 154 個站點）。blocker 2（hook 可達性）**設計解到、行號寫錯**：§3.4 寫『喺 :1231-1240 加 pinFocusScriptEdges』，但 :1231-1241 正是 `for (String rel : rels) { if (n >= 10) break; … }` 嘅 10 個名額迴圈，照字面插入就會被 jeiInfoScriptRels 餓死（同 R3 blocker 2 一樣）；§2 又寫『候選由新索引直接拎（唔經 rels 預算）』→ 兩句打架。必須明寫：pin 喺 :1241 之後、:1265 ranked 掃描之前嘅獨立迴圈。",
    "B6【Q4 第一半：成本 OK，可建】實測全 pack 648 檔：單 regex（registerCustomRecipe）5.5 ms、六合一 alt-regex 19.2 ms、全讀盤 49.9 ms；PackIndex 掃描點確係 :210-238（`isScriptPath` → `scriptRels.add` ＋ `indexScriptItems(rel)`），而 indexScriptItems 第一句就 `readText(rel)`（:1569）→ 每份 script 本來已讀入，加站點索引＝零額外 IO，成本可接受 ✓。但 §2 嘅『唔加 extra IO』成立嘅前提係要掛喺 :236-237，唔可以另開一次 walk。",
    "B7【Q5 falsifiability】S1 ✓（`grep -rn -- \"-\\[transform\\]->\\|-\\[ritual\\]->\\|-\\[recipe\\]->\" forge/1.19.2/src/main/java/` = 0 hit，今日必紅）、S2 ✓（naive ≥50 有餘裕）、S4/S5 ✓。但 S3 唔係有效測試：正向斷言只要見到 render 輸出就過，**方向反轉都會綠**（B1 情境），負對照 trivial（B5）；S6 精度政策自相矛盾（A 寫死精確 2、C 寫 ≥500／≥30 下限、B 寫死 8＋13）；S7③④ 實際上唔可證偽——③ 對最大真檔（224,407 B）跑六合一 regex 平均 **0.77 ms**，同 50 ms 門檻差 65 倍，除非故意寫 O(n²) 否則永遠過；④ 本 corpus 非 UTF-8 檔 = **0**、BOM 檔 = 1（startup_scripts/golden_age/ink_register.js）→ 只能用合成 fixture 測，唔係對真 corpus 可證偽。",
    "B8【Q6 新引入矛盾】① §0.2＋§4 S1 對 v4 §6⑥＋真 JEI 卡＋trace 自身規則（見 B1，最重）。② §3.7 自相矛盾：一邊寫『新索引寫入現有 index（唔寫 kjs-*.json）』，一邊要 bump `INDEX_SCHEMA_VERSION`（KubeJsMechanicScan.java:96 / :505 讀 / :531 寫）——該版本號守嘅係 kjs-*.json mechanic cache，新索引唔寫入就無資料理由，bump 只會迫一次無意義 cache 重掃。③ §3.4 行號 vs §2 語意（見 B5）。④ §5 通用性聲稱 vs §1 嘅 pack-local／單 mod 錨點（見 B3）。⑤ §4 S1 只列 C 一條 edge（necklace），但 §1 規則對 :142 會出兩條（necklace＋gate_pearl）→ 斷言同規則唔一致。"
  ],
  "counterexamples": [
    "server_scripts/b_a_d/recipe/goety_ritual.js:1（class 定義 `GoetyRitualRecipe(craftType, ingredients, activation_item, output)`）+ :142（arg2 被當輸出）＋ trace ask-20260915-230340-kubejs_god_bless_full_necklace.jsonl `[card:4] 魔法仪式 · 13 slots · 黑暗祭坛 满溢神恩项链+… -> gateways:gate_pearl role=uses` → Sink C 噴反方向 edge，而 §4 S1 就係斷言呢條。",
    "server_scripts/dlc/dlc_summon.js:36 `.itemOutput('kubejs:yuanshen')` 之後 :37 才 `.id('kubejs:yuanshen_recipe')`；同一個 `;`-statement（:15–:96，全檔只有 2 個 `;`）內另有 :51/:52、:64/:65、:76/:75、:92/:91 → owner 無唯一解。",
    "server_scripts/mrqx_extra_pack/mrqx_recipes/mrqx_recipes.js:245 `.id('mrqx_extra_pack:ritual_divine_pen')` ／ :259 `.itemOutput('mrqx_extra_pack:divine_pen')`；全檔 2,953 行 **0 個 `;`**、316 條鏈、86 `.id(`、68 `.itemOutput(` → 一個 statement 包 68 條輸出。",
    "server_scripts/b_a_d/recipe/weapon_infusion.js:2 `WeaponInfusionRecipe(base, addition, output)` → 9 個 site 各噴 2 條錯（base/addition 當取得途徑，共 18）；server_scripts/mayacraftdlc/recipe.js:1 `DragonForgeRecipe(input, blood, output, dragonType)` → 45 site × 2 = 90 條錯（例 recipe.js 區內 `Item.of('iceandfire:…')` 當 input 用）。",
    "server_scripts/b_a_d/recipe/cooking.js:2 `BADCookingRecipe(container, ingredients, output)`；:34 `BADCookingRecipe(Item.of('minecraft:bowl'), [ …Ingredient.of… ], Item.of('b_a_d:meat_rice'))` → arg0 容器（碗）被當成取得途徑（同類 container 錯 edge 共 70 條）。",
    "反面例（證明修法可行）：server_scripts/b_a_d/recipe/bio_forging.js:1 `BioForgingRecipe(ingredients, output)` → 123 個 site 嘅 arg1 全部正確對應 this.result；即『parse class 定義 → arg↦JSON 欄位』係可行且 pack-agnostic（class 定義就喺 corpus 內）。",
    "Sink A 正面例：server_scripts/curios/entity_death.js:21-27（key `'kubejs:god_bless_empty_necklace'`，:26 換入 Item.of('kubejs:god_bless_full_necklace')）＝全 pack 唯一真取得途徑之一；:15-16 `curiosDeathStrategies[item.id]` 證 key 就係來源物 id。",
    "未被覆蓋嘅同 pack 大類：`event.recipes.create` 622／`createDeploying` 293／`createPressing` 55／`createCutting` 48 次（28 檔）、`event.custom(` 77 次／40 檔、`\"result\":` 32 次／6 檔——換 pack 後主流寫法就係呢啲，三條 sink 一條都唔中。"
  ],
  "number_errors": [
    "「649 scripts」→ 錯（多 1）：`find -type f -name '*.js'` = **648**；第 649 個係**目錄** `server_scripts/maodlc/goety_recipe.js`（plan §8 自己都認咗係目錄），Java 端 PackIndex.java:211 `Files::isRegularFile` 已過濾 → 講『scripts』應該係 648。",
    "「max 207 KB」→ 錯：最大 .js = **224,407 B**（server_scripts/ino_dlc_build/ino_dlc_build_organ_active.js，219.1 KiB／224.4 KB）；207,288 B（202.4 KiB）係**第二大**（ino_dlc_build_driver_kick.js）。§4 S7③ 用 207 KB 做『極大 script』fixture＝測錯檔（而且該門檻 50 ms 同實測 0.77 ms 差 65 倍）。",
    "「registerCustomRecipe(new XRecipe( 566 次／38 檔」→ 數字對，但**pattern 同數字唔一致**：照字面 `new <name>Recipe(` = **509 次**（38 檔）；566 要寫成 `registerCustomRecipe(\\s*new <任何 class>(`（例 `new CreateAdditionCharging(`、`new mrqxCreateAdditionCharging(`）。另：`registerCustomRecipe(` 總數 614，其中 **47 個係本地函式定義**（`function registerCustomRecipe(recipeModel)`，40 檔），其餘 567 個全部帶 `new`（566 行內＋1 個跨行）。",
    "「6.42 MB」→ 單位含糊：6,733,301 B = 6.42 **MiB** = 6.73 MB(SI)。唔算錯，但要寫清 MiB。",
    "以下全部親核**成立**：setStackInSlot 5 次／4 檔（2 個 Item.of sink：entity_death.js:26、entity_hurt.js:36；另 3 個＝maodlc_key_pressed.js:291、dlc_common.js:61/67）；`.itemOutput(` 217 次／12 檔；`Item.of(` 3361 次／222 檔；`Ingredient.of(` 1282 次／40 檔；旗艦（server_scripts/ritual/summoning_rituals.js）splitlines 441、`.altar(` 29、`.itemOutput(` 8（8 條全裸字串）、`.mobOutput(` 21、`.sacrifice(` 7、`Item.of(` 1（:8）、`.id(` 29（ritual_* 7）、`.input(` 93、`recipeTime` 29＝`;` 29；第二檔（b_a_d/ritual/summoning_rituals.js）249 行、`.itemOutput(` 16 = 13 裸字串＋3 個 `Item.of('gateways:gate_pearl','{gateway:…}')`（:89/:118/:125）；constdef.js splitlines 326／47（wc -l 得 325／46，即 plan 用 splitlines，一致）；tests/check_*.py = 115；`kubejs:god_bless_summon` 2 次／2 檔；`kubejs:ritual_catalyst` 98 次／14 檔；`Item.of('kubejs:god_bless_full_necklace')` = 2 處（goety_ritual.js:142、entity_death.js:26）；`kubejs:ritual_god_bless_summon` = 0；band 註解 :1398；MAX_GRAPH=200(:28)／MAX_RETRIEVE_FACTS=24(:35,:563)；INDEX_SCHEMA_VERSION=1(KubeJsMechanicScan.java:96)。"
  ],
  "ratio": "ratio: 正方 6 : 反方 4（≈60% 存活：Sink A 規則、by-output 索引（成本實測 5.5–19.2 ms、零額外 IO、正確棄 inverted）、caps／toml-only kill-switch、§0.1 通道轉向、§8 幾乎全部更正、數字 12/14 準確；≈40% 站唔住：Sink C 核心規則（382/895 條方向反轉，含 §0.2＋§4 S1 旗艦斷言）、Sink B owner 界定（74/217 條無解）、§5『支援多 pack』、§2『只 parse 命中 statement』、§3.4 hook 行號／§3.7 schema 理由、S3/S7③④ 可證偽性）",
  "recommendation": "R4 係 AGENTS.md 容許嘅最後一輪，比值仍 6:4 未達 8:2 → **唔好收貨、唔好開 R5，直接停手問 SK**，並附以下卡死點：(1) 最貴嘅未知＝『邊個 arg 係輸出』——呢個唔可以由 Item.of 出現位置推出（真值證明 43% 錯），唯一 pack-agnostic 出路係 parse corpus 內嘅 JS class 定義（`function XRecipe(p1,p2…){ this.result = p… }`，本 pack 24 個定義、BioForgingRecipe 123 site 證明可行），class 無定義者一律**唔出 edge**（安全）而唔係猜；(2) 收窄版（今日就可以安全出貨）：只做 Sink A（2 個真 site、方向已用 `strategies[item.id]` 驗證、可斷言 entity_death.js:26 正／entity_hurt.js:36 負），Sink C/B 明寫『未支援』，唔出任何 recipe／ritual edge；(3) 若 SK 要保留全範圍：Sink B 改成『接收者鏈』界定（唔可以用 `;`，實測 ASI 會令 2953 行塌成一句）＋Sink C 改成 class-def 驅動，兩者都要過『方向真值閘』——用 trace 內 JEI 卡（`role=input/role=output`）做交叉核對，即『斷言方向』而唔係只斷言出現（現時 S3 對反方向 edge 都會綠）；(4) 唔准把 goety_ritual.js:142 當 Sink C 驗收例——同一件物喺 :435 `.altar(`、peifang.js:219 `.input(`、lunasexrecipes.js:997 `Ingredient.of(` 全部係輸入側，而真 trace 卡 `card:4 … 满溢神链… -> gateways:gate_pearl role=uses` 已經係現成反證。"
}
```