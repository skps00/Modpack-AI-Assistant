# R1 反方 review — item-info-completeness plan（v1）

> **記錄來源說明（誠實標註）**：本檔由 Hermes 將 subagent 輸出原文保存。R1 subagent 因 **iteration cap（50 次）而未及寫出佢自己嘅報告檔**，其完整 findings 見下方 JSON（原文照錄，未改寫）。
> 我已逐條親核（見 plan v2 §0.3）：全部成立。其中「81% 係高估」已由我修好儀器證實（真值 ALL 75%／detectable 90%）。

- 審查對象：`docs/plans/2026-09-19-item-info-completeness-plan.md`（v1，commit `7b12a17`）
- 結果：**正方 2 : 反方 8（go=false）**
- live transcript：`<HERMES_CACHE>/delegation/live/deleg_6d5871c5/task-0.log`
- subagent summary 原文：`<HERMES_CACHE>/delegation/subagent-summary-0-20260919_235348_424404.txt`

## subagent 原文（JSON）

```json
I ran out of tool iterations before writing the report file — that is an honest blocker I must flag, not paper over. Here is the outcome with the evidence I did gather.

```json
{
  "report_path": "docs/plans/reviews/2026-09-19_item-info-completeness-plan-R1-opposing.md",
  "report_written": false,
  "report_write_status": "NOT WRITTEN — iteration limit reached before the single permitted file write. All findings below are backed by real tool output collected from the repo and traces; the file itself does not exist on disk.",
  "scores": "正方2:反方8",
  "supporting_score": 2,
  "opposing_score": 8,
  "go": false,
  "baseline_reproduced": "NOT 81%. Running the plan's own instrument (`python tools/check_item_info_coverage.py`) yields 78% (126/161), not 81% (134/165). The script's numerator is buggy: line 28 builds `cov` from ALL CATS names found in the answer, then line 29/41 counts `len(cov)` — not the set-intersection with `avail`. My independent set-intersection gives 113/161 = 70%; the script inflated the numerator by 13 answer-only categories. Measured variants: tools+facts blob (plan's method) 70% (153 avail, 107 covered); tool.result only 70% (88 avail, 62 covered); blob minus the boilerplate-fed categories (script/tags/fishing/trade) 84% (108/129). Of the 12 categories, 6 are un-detectable-from-answer and must be excluded: script (kubejs internal), tags (triggered only by the literal tooltip line 'Hold §e§oCtrl§r§7 for Tags' in purpose_lookup — real #ns:tag ids appear in only 2/19 cases), fishing AND trade (both triggered only by the hardcoded `sources` boilerplate array in LlmClient.java:449-457 — 'pack loot / fishing / trades / scripts (if any)'; removing that key drops fishing availability from 19/19 to 0/19), worldgen (question-gated via AskEngine.java:695, not data-gated), mobdrop (1 real instance). So G1's '12/19 cases have the tags gap' is a detector artifact — real tag availability is 2/19 and BOTH already mention the tag in the answer, i.e. the true tags gap is ~0.",
  "top_blockers": [
    "CRITICAL — Baseline number is not honest and the instrument cannot be reused as-is. tools/check_item_info_coverage.py:28-41 measures len(answer-categories) as the numerator instead of |avail ∩ covered|; reproduced 78% where the true figure is 70%, and the plan's 81% is the same bug one revision earlier. A1 ('100% of available') sits on this broken denominator, and 'available' includes boilerplate-fed categories and the un-answerable 'script'/'worldgen' — so A1 is unfalsifiable: you can always reach 100% by shrinking 'available'. Pre-registered per-category numbers and a frozen denominator are mandatory. FLIP: provide a fixed 12-category whitelist with the intersection operator and show A1 red on tonight's traces.",
    "CRITICAL — D1 as written is not feasible: it needs structure names the pack index does not have. JarLightIndex.java has zero occurrences of structure|worldgen (grep, 0 hits); isLootEntry/lootKeyFromPath (JarLightIndex.java:171-192) yield a bare table id only. Verified against the real jar cache (config/packai/jar-cache, 360 files): ref values are `chests/village/moon/blacksmith`, `inject/chests/end_city_treasure` — never 'End City'. The one place a structure name appeared (AcquireAskTool output for tetra:dragon_sinew: `Loot: 掉落表「end city treasure」`) is a localized lang string (packai.reply.loot_table_obtain, en_us.json:472), not machine-readable. D1's `{kind:'loot', table:..., structure:'End City'}` therefore requires a brand-new tableId→structure mapping that is neither scoped nor whitelisted. FLIP: show the mapping as an existing index field, or remove `structure` from D1 and re-scope.",
    "CRITICAL — the plan's own premise that real loot data reaches the LLM is false tonight, and it under-counts the loss. `acquire` returned EMPTY in 16/19 cases (non-empty only tetra_dragon_sinew, thermalendergy_endergy_upgrade_1, mekanism_sps_casing; AcquireAskTool.java:42-43 returns \"\" on empty lines). `graphFacts` was `[]` in all 19 send.facts payloads. The JAR channel (JarLightIndex.factsForAsk, READ at AskEngine.java:272/292, assembled at AskEngine.java:481, inserted at AskEngine.java:692-693) contributed nothing: zero of 19 cases contain the '[JAR]' headline. Real counter-example: ad_astra:oxygen_tank has `L|chests/village/moon/blacksmith` in jar-cache, yet the answer states 'No loot, chest, trade or fishing path for it is indexed in the pack' — a factually wrong answer the plan does not account for. FLIP: show one trace where the JAR/loot route reached the answer text.",
    "HIGH — D1 has no budget model and the caps will silently eat it. Real payloads measured: 1478 / 1640 / 1696 / 2091 chars. Existing caps: MAX_ASK_LINES=4 and MAX_FACTS_PER_ITEM=8 and MAX_LOOT_PER_JAR=150 (JarLightIndex.java:56-60), MAX_ACQUIRE_LINES_FULL=12 (AskToolContext.java:31), maxFacts=24 (PackAiConfig.java:351), maxJeiChars=12000, all funnelled through appendCapped (AskEngine.java:692-693, :1324) and factCap. Also 224 of 6842 cached items already exceed 8 refs. Adding a structured acquireRoutes block without a stated per-round budget guarantees truncation of a different section. FLIP: state a concrete char/token budget per round and show the block survives appendCapped at maxFacts=24.",
    "HIGH — D2 (appended repair section) collides with card placement and the footer, which is exactly what the plan swears it will not touch. RecipeEmbed.java:747 documents 'Sources footer always last' and interleaveEmissionCards (RecipeEmbed.java:749-752) hard-splits on the 【來源】/[Sources] footer. Meanwhile AskEngine.java:930 calls AskJeiHints.ensureQuestStatusVisible, which anchors insertions via ReplySources.HEADER (AskJeiHints.java:274) and inserts BEFORE the footer. tests/check_ask_card_fallback.py:309 and :842 assert that trailing section prose must not swallow cards and that 'card marker must not sit inside trailing prose'. A deterministically appended 補充 block lands exactly in that boundary region. FLIP: name the insertion point relative to the footer and show check_ask_card_fallback.py stays green with a 補充 section present.",
    "HIGH — D2 language + path-leak rules are violated by design. D1 demands provenance '檔名:行號', but PackIndex.java:1284 carries the explicit comment 'no file/line (player-visible forbidden)'. Any repair section that renders D1 provenance leaks kubejs/server_scripts/playerhandler.js:40 to the player. Separately, a generated section needs lang keys, which means 3 files the whitelist omits. FLIP: state that the repair section re-humanizes provenance through ReplyLang and name the lang keys it uses.",
    "HIGH — whitelist/rollback is incomplete and its headline count is already wrong. `git status --porcelain` = 126 entries (71 M, 53 ??, 2 D), not 127. Missing from the whitelist and inevitably touched: logic/AskToolContext.java (all the budget constants live there), logic/AskToolLoop.java (CAPABLE_TOOLS / FIRST_ROUND_TOOLS / MAX_LLM_ROUNDS=3; registering tags_lookup requires it), logic/JarLightIndex.java, logic/LootForwardIndex.java, config/PackAiConfig.java (plan §6 promises a config toggle for the tags cap → new key → 3 lang files → tests/check_settings_registry.py), and assets/packai/lang/{en_us,zh_cn,zh_tw}.json. tests/check_tool_schema_stable.py:108 asserts `forge_only == [\"knowledge_lookup\"]`, so adding tags_lookup turns a python gate RED by design — that is a NEW red, not the plan's 'known 1 red'. And research/gen_tmp_check.py regenerates forge/1.19.2/tmp-check.gradle (untracked, `??`): 93 *Check.java exist but only 50 run*Check tasks are registered, so A4's '50/50' is stale. FLIP: extend the whitelist to those exact paths and pre-register the new expected red.",
    "MED — D3.2 is a REAL bug, not a best-match artifact (plan is right, but understates it). All 5 guide_fetch calls: 3 return the identical unrelated page `worn_notebook/enchantments/how_to_enchant` (for minecraft:end_portal_frame, quark:chiseled_soul_sandstone, ad_astra:desh_engine), while the 2 correct ones (modularrouters_puller_module_1, advancedperipherals_inventory_manager) resolved properly. Root cause verified in code: PatchouliGuideLookup.java:55-63 'B2: title search only when item path empty' → searchByTitle(question,...), and the question template embeds the display name, so generic words ('Enchant') match titleTokens. Confirmed from the real index (entryCount 1759): ZERO entries mention end_portal_frame, so empty was the correct answer. Bonus unmentioned defect: the returned entry's title/textClip are raw lang keys ('ars_nouveau.page.how_to_enchant'), i.e. the guide index serves untranslated keys as body.",
    "MED — A2/A6 cannot run as specified. tools/cardplace_sampler.py has no --mode at all: its docstring declares two subcommands (pools/draw) and argparse only defines --per-cat/--random-n/--seed/--pinned (lines 264-267). '--mode loot-only' does not exist, and `pools` harvests item models/lang from jar assets, not loot tables, so it holds no signal for 'no recipe output AND loot table hit'. Its GAME default is C:/Users/skps9/Documents/packai_dev_game, not the FTB sandbox. FLIP: show `--mode loot-only` implemented and the sampler pointed at the sandbox via PACKAI_GAME_DIR.",
    "MED — A5 is an invalid negative control and A1/A2 lack pre-registered numbers. Baseline already covers ~70% of real tool-available categories with zero D2, and the 19 traces show the model spontaneously emitting 'Obtain notes:', 'How to use:', '[Machine]' sections. Removing D2 would therefore drop a *regex-measured* number, which proves only that the instrument measures D2 — not that D2 is load-bearing. FLIP: pre-register the exact per-category delta (e.g. tags +11→0, guide +5→0) that A5 must produce, and name which assertion flips red.",
    "LOW — G3 misdiagnosis. No answer-length cap exists in code (grep MAX_ANSWER|maxAnswer|bodyCap|answerCap → 0 hits). Measured 19 answers: 1262–2012 chars (min 1262 is below the plan's claimed 1.3k floor). So there is no cap to 'raise'; G3 is model behaviour, not an implementation ceiling.",
    "LOW — reproducibility claim is loose but directionally right. Two tetra:modular_double runs: difflib ratio 0.646, word-token Jaccard 0.681 (plan said '65%'). Facts and tool-call sets did match, so the plan's substantive conclusion (wording, not information loss) survives my check."
  ]
}
```
```
