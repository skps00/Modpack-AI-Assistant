# 反方報告（R1 opposing）— P0 `QuestGuide.stripQuestIcons` 嵌套 icon 崩潰修法計劃

- 對象：`docs/plans/2026-09-19-p0-questguide-stripicons-crash.md`（commit `27d5d0e`，verify：`git log -1 --oneline -- <plan>` → `27d5d0e docs(plan): P0 QuestGuide.stripQuestIcons nested-icon crash …`）
- Reviewer：Hermes（反方）／日期：2026-09-19 晚
- 版本界線：`forge/1.19.2`（Forge 43.4.5、MC 1.19.2、`mod_version=0.2.3`）；`neoforge/1.21.1` PAUSED，不審。
- **只讀聲明**：本輪我**冇改、冇新增、冇刪除 repo 內任何檔案**（唯一新增 = 本報告）。我冇 build、冇跑 gradle、冇跑 `tests/check_*.py`、冇 commit、冇部署、冇開遊戲。所有 probe 腳本寫喺 `%LOCALAPPDATA%\Temp\adversarial_probe{,2,3,4}.py`（repo 外），讀檔全部唯讀。
- 已核實／推斷分開標示；跑唔到嘅一律寫 **NOT RUN**。

---

## 總分

**正方 6 : 反方 4 ｜ go = false**

理由壓縮成一句：**技術核心（根因＋D1）我獨立重現到、而且係對嘅**；但**驗收標準可以被 D2 單獨滿足（假綠）**、**§7 還原方案嘅前提同實際工作樹狀態相反（仲有一條會摧毀未提交工作嘅路徑）**、**T3/T4 預期值根本冇寫死到可斷言**——P0 修法嘅「點樣先算做完」係穿窿嘅，唔可以開工。

---

## 我親手做過嘅驗證（evidence base，全部可重跑）

| # | 動作 | 結果 |
|---|---|---|
| V1 | `grep -c "AskEngine failed" %TEMP%/autotest_results_20260919-213821/latest.log` | **19** ✔（同 plan 一致） |
| V2 | 睇 `latest.log:41067-41079` | `IndexOutOfBoundsException: start 742, end 696, length 1214` ← `QuestGuide.stripQuestIcons(QuestGuide.java:1538)` ← `itemsInRange:1471` ← `parseQuestsArray:1013` ← `parseFile:947` ← **`QuestGuide.lambda$index$2(QuestGuide.java:142)`** |
| V3 | `grep -o "IndexOutOfBoundsException: start …" \| sort \| uniq -c` | **1×`(742,696,1214)`＋18×`(758,710,1259)`**（`Query failed:` 字串共 38 行 = 每次失敗 2 行） |
| V4 | `grep -c model.reply latest.log` | **0** ✔ |
| V5 | `status-20260919-134134.json` | `caseCount=20`、`ok=19`、`status=DONE`、`cardsOut` 5/3/2 |
| V6 | 自己寫 Python port（忠實移植 `stripQuestIcons`＋`topLevelObjects`），**逐個 top-level quest object 切片**跑 | 精確重現 runtime 簽名：`getting_started.snbt` **obj#26 len=1214 → last=742 start=696**，`id="4EFD411CA5975754"`、`title="Starting Your Island"`；**obj#81 len=780 → (466,415,780)**，`id=4697678CA1F15CD6`、`title="Viewing Dependencies"` |
| V7 | 逐 slice 掃三包 | FTB **2/755 slices 崩**（file-level 1/51）、E9E **0/1328**、主包 **0/2517** |
| V8 | D1 守衛喺**真檔真 slice** 嘅輸出 | `output == input 減去恰好 1 個 icon-block span`（116 / 121 chars）；殘留 `icon:` key = **0**；`ftbteams`/`custom_icon` 殘留 = **False**；抽到 item `ars_nouveau:annotated_codex`（obj#26）、`farmersdelight:kelp_roll_slice`（obj#81） |
| V9 | 三包 FTB Quests jar 檔名 | ftb＝`ftb-quests-forge-1902.5.10-build.497.jar`；**e9e 亦係 `1902.5.10-build.497`**；主包＝`1902.5.9-build.399.jar` |
| V10 | 真 instance jar sha256 頭 16 | `06b5b129a114a233…` ✔（同 plan 一致） |
| V11 | `git status --porcelain \| wc -l` | **127**（工作樹唔係乾淨） |
| V12 | 白名單檔狀態 | `AskReplyScrub.java` = **M**（已有未提交改動）、`OfficialDisplay.java` = **?? untracked**、`QuestGuide.java` = 乾淨、`tmp-check.gradle` = untracked |
| V13 | `tmp-check.gradle` 條目數 vs `find src/test -name '*Check.java'` | **49 == 49** ✔；檔頭寫明 `// AUTO-GENERATED by research/gen_tmp_check.py`，而 `research/gen_tmp_check.py` 係 `rglob("*Check.java")` 自動收集 |
| V14 | `ls tests/check_*.py \| wc -l` | **123**（plan 講 122 綠＋1 已知紅 ⇒ 分母對得上；**我冇跑**，見 NOT RUN） |
| V15 | `git show HEAD:...AskReplyScrub.java \| grep -n "m.start() < last"` | **HEAD:1433 已經有同一守衛**（"nested/overlapping marker already consumed by brace-depth skip"） |
| V16 | `git show HEAD:...QuestGuide.java \| sed -n '1529,1545p'` | HEAD 版 `stripQuestIcons` **無守衛** ✔（＋ `git log -5 -- QuestGuide.java` 最新 commit = `c0365bb`，檔乾淨 ⇒「已發佈版本同樣有」由原始碼即可推出） |
| V17 | 狀態空間矩陣（18 個輸入）跑 current vs D1 | 詳見 §F4／§附錄 |
| V18 | `getting_started.snbt` mtime | **2026-09-19 21:40:54**（= 失敗窗口 21:40:53–21:41:34 之內） |

---

## 發現（findings）

### F1【CRITICAL】驗收標準可以被 **D2 單獨**滿足 → 「假綠」風險
- 證據：`status-*.json`（V5）顯示 autotest **`ok=19`、`cardsOut` 5/3/2**，但同一次 run 19/20 個答案都係 `Query failed`、`model.reply=0`（V1/V4）⇒ **harness 嘅 `OK` 唔等於「有真答案」**。再看 A4 三條要求（「≥90% 有真 LLM 答案；body 零 `Query failed`；tokens 增量 > 0」）：只加 D2（逐檔 fail-soft）＋唔加 D1，正好**全部滿足**——因為 `AskEngine.ask:263` 每次問都 `QuestGuide.index(...)`（`AskEngine.java:263` 逐步讀過，無 index 快取），唔再拋 ⇒ 每個 case 都會行到 LLM、有 `model.reply`、tokens > 0、冇 `Query failed`。但失去嘅係：`getting_started.snbt` 兩個任務（`4EFD411CA5975754`、`4697678CA1F15CD6`）及其 item（V8）**永遠唔會入 index**，而 A1–A7 **冇一條**要求「呢兩個任務／呢個檔嘅內容真係被索引到」。
- 反方主張：呢個係 P0 修法最嚴重嘅洞——**plan 可以「全綠」而個 bug 只係由「大聲爆」變成「靜默消失」**。必須加一條機械可驗嘅 A8，例如：headless harness 用 fixture gameDir 跑 `QuestGuide.index()` → assert 出到 `questId=4EFD411CA5975754` 且 `items` 含 `ars_nouveau:annotated_codex`；真機 layer 另加「trace 內 ≥1 個 case 嘅 quest hits 非空」。並且明確寫「**唔准用 `status.ok`／`cardsOut` 當證據**」。

### F2【CRITICAL】§7 還原方案前提同現實相反，而且有一條會**摧毀未提交工作**
- 證據：V11 `git status --porcelain | wc -l` = **127**；plan §7 寫「改動前：`git stash list`／`git diff` 為零 → 只需 `git checkout -- <白名單檔>` ＋ `rm` 新測試檔即可回到現狀」。實際：
  1. `AskReplyScrub.java` **已經有未提交改動**（V12，` M`）→ 佢係白名單 #4（條件式）→ 一旦 D3 證實要改而事後用 `git checkout -- AskReplyScrub.java` 還原，**會連原有未提交改動一齊清走**（V15 亦證 HEAD 1433 與 worktree 1479 係兩個版本，即 worktree 有未提交內容）。
  2. `OfficialDisplay.java` 係 **untracked（`??`）**，即從未入 git → `git checkout -- forge/…/OfficialDisplay.java` 會回 `error: pathspec … did not match any file(s) known to git`，**還原方案對白名單 #4 完全失效**（要 `rm` 或事先備份）。
  3. 另外工作樹有 2 個**已刪除**檔（`PackAiSettingsScreen.java`、`RecipeCategoryScreen.java`）同大量 M/??（127 項）——「回到現狀」呢個概念本身要重新定義（現狀 ≠ HEAD）。
- 反方主張：還原方案係 SK 第一規則（先確認可還原才動手）嘅核心，唔可以建基於一個唔成立嘅前提。最小要求：動手前 `git status --porcelain > baseline`（另存時間戳檔）；白名單每個檔**先 copy 一份到 `%TEMP%/`**；明文寫「唔准用裸 `git checkout -- <白名單檔>` 還原，除非該檔改動前已記錄為 clean」。`QuestGuide.java` 呢邊冇問題（乾淨 ✔），所以實際風險集中喺 #4。

### F3【HIGH】A7「白名單外零改動（`git diff --stat` 核）」在 127 項 dirty tree 下**不可驗**
- 證據：同上 V11；`git diff --stat` 今日已經有幾十個檔，A7 用「diff 為零」做判準會**永遠紅**，用「有 diff 就算」又等於冇查。V12 亦顯示 `src/test` 下有 5 個已提交檔被改、20+ 個 untracked 測試檔——「新檢查檔之外仲有乜變咗」用肉眼分唔到。
- 反方主張：A7 必須改成「同 baseline 清單**逐行 diff**（`comm`／`diff`），只准出現白名單檔（＋時間戳），任何其他新增/修改/刪除 = FAIL」，並且要指名用邊條命令、產出邊個檔。順帶：**A7 只覆蓋 `forge/1.19.2`？冇寫**——`tests/check_quest_strip_icons.py`（見 F8）喺 repo 根，A7 字面上唔包。

### F4【HIGH】D1 嘅**語意宣稱被否證**：畸形輸入會由「崩」變成「靜默吞正文」，零 log
- 證據（V17 矩陣，我自己 port 跑）：
  - `'head icon: { Count: 1b tag: { Icon: "a" item: "minecraft:stone" tail icon: "b" end'`
    → current：**CRASH(82>30)**；D1：輸出 = **`'head '`** —— `item: "minecraft:stone"`、`tail`、`end` **全部無聲消失**。
  - `'head icon: { Count: 1b item: "minecraft:stone" tail'`（無後續 match）→ **current 一樣係 `'head '`**（無 crash，已存在嘅同類截斷）⇒ 呢類截斷係**既有行為**，D1 只係令佢由「偶然唔爆」變成「永遠唔爆、永遠靜默」。
  - plan §3 寫「跳過係**語意正確**（唔會漏剝應剝嘅裝飾 icon，亦**唔會多剝正文**）」→ 對 well-formed SNBT 我**實證成立**（V8：真 slice 減去恰好一個 block，殘留 0），但對畸形 SNBT **唔成立**。
- 反方主張：D1 個 diff 我唔反對，但 plan 要：(a) 改字眼為「well-formed 正確；unterminated `{` 會截尾（既有行為，D1 令其靜默）」；(b) **加一行診斷**（skip 時 `LOGGER.debug` 帶 offsets，或最少留註釋＋REMAINING_WORK 條目）；(c) 喺 T3 加一條 unterminated 案例，並寫死期望字串（我提供：`'head '`），令呢個「已知語意損失」有回歸保護而唔係靜默漂移。

### F5【HIGH】T3／T4 預期值含糊到**唔可斷言**，而其中兩個子案例今日係「錯 output」唔係「無殘留」
- 證據（V17，全部 current == D1，即 D1 冇改過呢兩個行為）：
  - T3「`icon:` 喺字串內」：`'desc: "text icon: more" item: "minecraft:stone"'` → 輸出 **`'desc: "text more" item: "minecraft:stone"'`** ⇒ 字串值內嘅 `icon:` **真係被當 icon 剝走、字串被改爛**。
  - 「`icon:` 喺字串最尾」：`'desc: "see icon:"'` → 輸出 **`'desc: "see '`** ⇒ 收尾引號同後文一齊無咗。
  - plan §4 T3 只寫「輸出無 `icon` 值殘留（除字串內者，**另列期望**）」——「另列期望」= 未定；而 plan §3 又把呢個情況講成「可接受」。**同一個輸入，寫測試嘅人可以寫成「期望 = 原字串」而紅，亦可以寫成「期望 = 改爛版」而綠**，兩種都「符合 plan 字面」⇒ 呢條唔係回歸閘。
  - T4「正文逐字保留」亦唔精確：真實形狀 `'pre icon: { … } post'` → 輸出係 **`'pre  post'`（雙空格）**，唔寫死就會爭議。
  - T2 期望 = **`' tail'`（前置空格）**；T3e `'icon: }'` → **`'}'`**；T3f `'{ icon:{ Icon:"x" } }'` → **`'{  }'`**；`'icon:'` → **`''`**；T3b 見上。全部要逐條寫死。
- 反方主張：T1–T5 每一格都要有**逐字 expected string**（唔准「正文保留」「無殘留」呢類形容詞）；字串兩條要明確標成「**既有缺陷、唔喺 P0 範圍**，期望 = 改爛版（附理由）」否則將來冇人分得清紅係 regress 定 expectation 錯。另加 T4 用**真 slice fixture**（見 F10）。

### F6【HIGH】§4 負控證明唔到 T3「偵測到 bug」
- 證據：V17——字串兩條、`'icon: }'`、`'icon:'`（位置 0）、`'icon: 5'` 呢幾個變體，**移除 D1 之後輸出完全一樣、亦唔會拋**：即佢們**永遠綠**。所以「暫時移除 D1 → T1/T3/T4 應變紅」對 T3 只成立於**嵌套子集**。
- 反方主張：負控要逐條點名「邊條 assertion 會翻紅」：T1、T3 嵌套子案例、T3f、T4、真 slice fixture；並且要**明文記錄**「字串子案例本來就唔會翻紅（因為佢唔經 `start<last` 分支）」。否則負控通過（紅→綠）只證明「有測試會紅」，唔證明**每個**宣稱嘅案例都測到 bug。

### F7【MED】D2 範圍／logging 要求太虛，而且**冇驗收項**要求 fail-soft 本身有測試
- 證據：V2 stack 顯示爆點路徑係 `lambda$index$2(QuestGuide.java:142)`，即拋出係喺 `index()` 內 `Files.walk(...).forEach(p -> { … })` 個 lambda（`QuestGuide.java:123-153`）；該處**現有 try 只 catch `IOException`**（`:150`）。plan 寫「喺 `index()`（`:95`／`:123`）逐檔 try/catch」——`:95`/`:123` 係公開 overload，**喺嗰層 catch 唔係 per-file**（等於整個 walk 一次 fail-soft，之後嘅檔案全部唔會再讀）→ 同 plan 自己嘅需求（單一壞檔唔可以拖死其他檔）相反。
- 反方主張（我要求嘅精確 catch scope ＋ logging，缺一不收）：
  1. catch 必須**包住個 lambda body**（`:137`–`:152`：`Files.size`、`Files.readString`、`parseFile`、`byId/noId` 插入）：`catch (RuntimeException e)`；**唔准** catch `Throwable`（OOM/SOE 要照爆）。
  2. log **每檔最多一次**：`WARN` 用 `p.getFileName()` ＋例外 class ＋ message（唔准印檔內容／答案），stack 落 `DEBUG`；並且要有一個**可數嘅 counter**（例如 trace event `packai.index.skippedFiles=N` 或 status 欄位），否則「靜默缺檔」永遠冇人知——直接違反 SK「唔准 fake success」。
  3. 要寫明 partial state 影響：`parseQuestsArray` 會喺拋之前往 `spoilerIds` add（`:1004`）而 `Hit` 全部丟（`:1017` 嘅 `out` 未 return）⇒ 該檔**全檔命中消失**（全有或全無），但 `spoilerIds` 可能殘留半截 → 需一行註釋交代（`filterHidden` 靠 id 唯一，實務無害，但唔寫就係未分析）。
  4. 加一條 harness 驗 fail-soft 本身（故意寫一個壞 .snbt 入 temp gameDir → 其餘檔仍索引到、counter=1），並列為驗收項；否則 D2 係永遠冇測試嘅新分支。

### F8【MED】白名單漏咗 **Python 鏡像** `tests/check_quest_strip_icons.py`
- 證據：`tests/check_quest_strip_icons.py:11-62` 係 `stripQuestIcons` 嘅**第二份實作**（mirror），**冇守衛**，現時只測非嵌套案例（`:86-93` 兩個 assert）。佢係 `tests/check_*.py` 一員（123 個之一）→ A3 會跑佢。D1 之後 Java 有守衛、Python mirror 冇 → **永久漂移**；而任何人日後喺 mirror 加一條嵌套 case 就會炸；A3 嘅「0 新紅」兩種情況都測唔到漂移（唔加 case 就一直綠）。
- 反方主張：要麼把 `tests/check_quest_strip_icons.py` 納入白名單（同步守衛＋加嵌套 assertion），要麼明文寫「已知漂移，記入 `REMAINING_WORK`，唔會同步」＋喺 plan §6 註明。現時白名單 4 條檔**完全冇提**呢個檔，等於默認放任。

### F9【MED】新檢查註冊喺 **untracked、自動生成**嘅檔案 → 檢查會消失、亦冇 CI
- 證據：V13——`tmp-check.gradle` 頭一行 `// AUTO-GENERATED by research/gen_tmp_check.py`，而 generator 係 `rglob("*Check.java")` 自動收集（49 == 49）；同時 `forge/1.19.2/AGENTS.md`「唔准 add 嘅檔」明列 `tmp-check.gradle`。plan §4 只講「加入 `tmp-check.gradle` → 49 → 50」。
- 後果：(a) 任何人跑一次 generator（或 generator 由另一份清單重建）就會**覆蓋**手改；(b) 呢個閘只喺本機存在，新 clone／CI／其他 agent **完全跑唔到**；(c) plan 冇寫 task 名（應該係 `runQuestGuideStripIconsCheck`）、亦冇寫 harness 契約（house style = `main()` ＋ `assert`（靠 `jvmArgs '-ea'`）＋失敗拋 `AssertionError`、尾行 `System.out.println("… OK")`；`build.gradle` 內**冇 junit/testImplementation**，見 `QuestGuideIdCheck.java:1-30, tail`）。
- 反方主張：A2 要明寫「跑 `python research/gen_tmp_check.py` 重新生成（或註明手改必須與 generator 同步）」、指名 task、寫死 harness 契約（main/assert/-ea/OK 行）、並喺 §7／REMAINING_WORK 記「此閘 local-only，冇 CI 覆蓋」。

### F10【MED】證據**唔完全重現得到**：plan 引用嘅係 1/19 嘅少數簽名，仲漏咗第二個 slice
- 證據：V3——真正 18/19 次失敗係 `(758,710,1259)`，plan §1 只引 `(742,696,1214)`（出現 1 次）。我掃**整個** `config/ftbquests`（51 個候選檔，同 `quests/**/*.snbt` 完全同一集合 → plan 嘅 glob 冇漏檔）**重現唔到 1259 簽名**，只重現到 `(742,696,1214)` 同 plan **從未提及**嘅第三個 slice `(466,415,780)`（V6/V7）。而 V18 顯示章節檔 mtime `21:40:54` **正正在第 1 次失敗（21:40:53.377）同第 2 次（21:40:59.011）之間** → 呢個檔案喺 run 中途被（FTB Quests）重寫過，令兩個簽名唔同源。
- 後果：plan 講「19x `Query failed: start 742…`」實際上唔啱（19 次入面 18 次係另一個簽名，而且今日已經驗唔到）；「1/51 檔」係**檔層**指標，真正單位係 **2/755 個任務 slice**（2 個任務、3 個簽名）。
- 反方主張：寫死 fixture（把 obj#26 嘅 1214 字 slice ＋ obj#81 嘅 780 字 slice 存入 `src/test/...` 或 inline text block），並喺驗收記錄三個 quest 檔 sha256；plan §1 要列出全部簽名。唔係吹毛求疵：**冇 fixture 就冇「同一輸入」可言**（見 F11 嘅 A5 亦要靠 baseline 數字）。

### F11【MED】A5／A4 量度未 pre-register（「同一量級」唔係判準）
- 證據：A5 原文「主包 `adjacentCardPairs` 樣本率同今次基準（54/316）**同一量級**（±唔准惡化到 0 或倍增）」——「同一量級」＋「唔准倍增」係修辭，唔係數值區間；而 **E9E 完全冇 baseline**（plan 只寫「每案例有真答案」）。A4「有效案例 ≥90% 有真 LLM 答案」嘅「真答案」冇定義，而 V5 已證 `status.ok`／`cardsOut` 唔可以當證據。
- 反方主張：要 pre-register 數值帶，例如 A5 主包 `adjacentCardPairs` ∈ **[48, 60]**（54/316 = 17.1% → 對應 15.2%–19.0%），E9E 先訂 baseline 後寫帶；A4 定義為「**20/20 個 case 各自 trace 內 ≥1 個 `model.reply`** ＋ tokens 增量 > 0 ＋ body 零 `Query failed`」（分母要寫死 20，唔准「有效案例」呢個可伸縮集合）。

### F12【LOW】D3 四條其實**即刻可以結案**，同埋佢個清單唔係 repo-wide grep 得出
- 證據（我逐條讀過）：
  - `AskReplyScrub:1482`：**HEAD:1433 已經有同一守衛**（V15）→ 「要驗」係過時；順帶係好證據：同一個 fix pattern 早已經俾 codebase 接受，**支持 D1**。
  - `AskReplyScrub:1420`（`shiftLeadingList`）：`last = m.end()`（`:1425`）＋ `find()` 遞增 ⇒ `last <=` 下一個 `m.start()`，**結構上安全**。
  - `AskReplyScrub:1227`：spans 由 `howToGetSpans` 產生，`from = Math.max(start + 1, end)`（`:1246-1248`）⇒ spans 遞增且唔重疊 ⇒ `sp[1] <=` 下一個 `sp[0]`，**安全**。
  - `OfficialDisplay:175`：`absStart = matchStart + localStart`（`:270-271`，`localStart >= 0`）→ `absStart >= m.start() >= 上一個 last`，**結構上安全**（唔需要守衛）。
  - 但 repo-wide grep 仲有同形站點未列：`RecipeEmbed.java:405,418,560,600,1655,1665`（全部 `last = m.end()`，安全）→ 顯示 D3 個 4 條名單係「心水清單」而唔係 grep 全量。
- 反方主張：唔算危險，但 plan 應該一次過寫「D3 = 5 處 grep 掃描，逐條 verdict（安全／已守衛／無證據唔改）」，並加一條驗收「D3 所有項有 verdict」；否則 D3 呢個 work item 冇「完成」定義（A1–A7 冇一條提 D3），做唔做都算收貨。

### F13【LOW】版本紀律寫法會誤導：版本**唔係**判別因素，內容才是
- 證據：V9——E9E 沙盒用**同 FTB 完全一樣**嘅 `ftb-quests-forge-1902.5.10-build.497.jar`，但 0/1328 slice 崩（V7）⇒ 判別因素係 **pack 內容（有冇嵌套 `tag:{ Icon: … }`）**，唔係版本。plan §1 寫「主包 FTB Quests 1902.5.9（無嵌套 → 不觸發）⇒ **跨版本／跨 pack 內容差異**，結論唔可互套」→ 讀落好易變成「1902.5.9 安全、1902.5.10 中招」。
- 反方主張：改成「**同一 FTB Quests 版本（1902.5.10）**，FTB Skies Expert 中招、E9E 唔中招 ⇒ 版本無關，內容相關；主包係另一版本（1902.5.9）但同樣係內容唔觸發」。版本數字本身係好事（保留），但推論方向唔可以錯。

### F14【LOW】port／mirror 保真度：Python `isspace()` ≠ Java `Character.isWhitespace`
- 證據：`'icon:\u00a0{ Icon: "a" }'`（NBSP）→ 我個 port **報 CRASH(19>8)**，但 Java 側 `Character.isWhitespace('\u00a0')` = **false** ⇒ 唔會入 `{` 分支、`last = m.end()`，**根本唔會爆**。即 Python port（同 `tests/check_quest_strip_icons.py:20` 鏡像）對 Unicode 空白嘅判斷同 Java 有差異。另外 Java 嘅 `length()`/offset 係 UTF-16 code unit，Python 係 code point（呢個檔 0 個 non-BMP，所以今次 V6 數字完全對得上；但唔可以當一般規律）。
- 反方主張：plan §1 要註明「port 係近似，差在 whitespace 類（NBSP/U+2007/U+202F）同 UTF-16 offset」，或者把 predicate 寫成 Java 一致嘅顯式集合。今次 `1/51` 我有獨立驗證（V7 同意），所以**唔影響結論**，但「已驗」嘅招牌唔應該蓋喺一個已知會 diverge 嘅工具上。

---

## Flip conditions（要改成咩，我會翻 go = true）

1. **F1（必須）**：加 A8 並寫死期望：headless harness 用 fixture gameDir（`config/ftbquests/quests/chapters/*.snbt`）跑 `QuestGuide.index()` → assert 命中 `4EFD411CA5975754`（"Starting Your Island"）同 `4697678CA1F15CD6`（"Viewing Dependencies"），並 assert items 分別含 `ars_nouveau:annotated_codex` / `farmersdelight:kelp_roll_slice`；再加「真機 layer 至少 1 個 case 嘅 quest hits 非空」。明文寫「`status.ok`／`cardsOut` 唔算證據」。
2. **F2（必須）**：§7 改成「動手前 `git status --porcelain > %TEMP%/baseline-<ts>.txt`；每個白名單檔先 copy 備份；**唔准**對已有未提交改動或 untracked 嘅白名單檔用裸 `git checkout --`」；並把白名單 #4 寫成「需先備份 `AskReplyScrub.java`（已 M）／`OfficialDisplay.java`（untracked），還原 = 還原備份」。
3. **F3（必須）**：A7 由「`git diff --stat` 核」改成「同 baseline 逐行 `diff`，只准白名單檔（＋baseline 自身）變動；其他任何 M/??/D = FAIL」，並註明覆蓋範圍包括 `tests/`（因 F8）。
4. **F5＋F6（必須）**：把 T1–T5 每格嘅**逐字 expected string** 寫落 plan（可直接抄我 §附錄）；字串兩條標「既有缺陷、期望 = 改爛版，附理由」；負控改為逐條點名會翻紅嘅 sub-assertion，並明文寫「字串子案例本來唔翻紅」。
5. **F4（必須）**：plan §3 承認 unterminated `{` 會截尾，並加一條 diagnostic（skip 時 debug log 帶 offsets）；T3 加 unterminated 案例（期望 `'head '`）。
6. **F7（必須）**：D2 寫死 catch 位置（lambda 內 `:137-152`）、只 catch `RuntimeException`、每檔一次 WARN（path＋class，唔印內容／stack 落 DEBUG）、加 skippedFiles counter，並加一條 fail-soft harness 驗收項。
7. **F10（必須）**：加入 checked-in fixture（兩個真 slice）＋三包 quest 檔 sha256；plan §1 列全部簽名 `(742,696,1214) / (758,710,1259) / (466,415,780)`，並把指標由「1/51 檔」改成「2/755 slices」。
8. **F11（必須）**：A4 寫死 20/20 ＋ `model.reply` ≥1/case ＋ tokens > 0 ＋ 零 `Query failed`；A5 主包寫數值帶（建議 [48,60] pairs）＋先訂 E9E baseline。
9. **F9（應該）**：A2 指名 `runQuestGuideStripIconsCheck`＋「跑 `research/gen_tmp_check.py` 重生」＋harness 契約＋「local-only，冇 CI」聲明。
10. **F8（應該）**／**F12／F13／F14（應該）**：白名單明確交代 python mirror 去向；D3 逐條 verdict ＋ 加驗收；版本推論改寫；port 保真度註明。

以上 1–8 全部係**改 plan 文字／加驗收**，唔使改 D1 設計（D1 本身我實證冇問題）→ 成本約 30 分鐘，唔需要重開設計。

---

## 我認為正確、唔會改（正方立場）

1. **根因判定（nesting → `last > m.start()` → `append(text,last,start)` 拋 IOBE）＝ 正確**，而且我**獨立精確重現**：真檔真 slice，`start 742, end 696, length 1214`，連 quest id／title 都對得上（V6）。plan §2 逐行引文同 `QuestGuide.java:1529-1589` 完全一致。
2. **D1 係最小且正確嘅修法**：well-formed 輸入下，我實測（V8）輸出 = 輸入減去**恰好**一個 icon block（116/121 chars），零 `icon:` 殘留、零裝飾 icon 殘留、任務 item 抽得到 ⇒ 唔會漏剝、唔會多剝。真 slice 嘅 `obj#26/#81` 兩條都係咁。
3. **D1 唔會留 icon 喺輸出**（因為 `last` 只會單調前進，被跳過嘅 match 必定落喺已丟棄區間內）——plan §3 末句「結構上唔可能再出現 `start < last`」成立。
4. **D3 原則「冇證據唔改」正確**，而且 1482 已守衛（V15）證明呢個 fix pattern 喺 codebase 有先例。
5. **版本紀律嘅做法（記 jar 檔名）正確**：FTB `1902.5.10-build.497`、主包 `1902.5.9-build.399`、真 instance jar sha `06b5b129a114…` ✓（V9/V10）；毛病只在推論方向（F13）。
6. **「已發佈版本同樣有」＝ 成立**：`git show HEAD:…/QuestGuide.java` 確認 HEAD 版 `stripQuestIcons` **無守衛**（V16），而該檔自 `c0365bb` 之後未改、工作樹乾淨。（jar 反編譯我冇重做，見 NOT RUN——但原始碼證據已足夠。）
7. **D2 嘅存在理由（單一壞檔唔可以拖垮全部答案）正確**：`AskEngine.ask:263` 每次問都重建 index（無快取），一個檔爆 = 19/20 case 全滅（V1/V2/V5）——加固係合理嘅。
8. **唔准 hot-copy jar、唔准部署真 instance、沙盒 only、唔准 commit** 等邊界寫得清楚，我唔會改。
9. **T5（`null` → `""`）同 T2 負控方向正確**：`stripQuestIcons:1530-1531` 對 `null` 回 `""`；T2 輸出 = `' tail'`（icon 走、tail 留）我實測一致。

---

## NOT RUN（我冇做／做唔到，唔會當已驗）

- **冇跑** `./gradlew.bat compileJava compileTestJava`、**冇跑**任何 Java harness（A1／A2 未驗；我冇 build 權限亦唔應該 build）。
- **冇跑** `tests/check_*.py`（唯讀要求；A3 嘅「≥122 綠 ＋1 已知」**未經我驗證**，我只驗到檔案數 = 123）。同時要指出：`forge/1.19.2/AGENTS.md` Gotcha 仲寫住舊 baseline「110/110」（2026-09-15）→ 兩個數字唔一致，plan 應該**貼實際 baseline 輸出**（逐檔名 + RC）而唔係寫「≥122」。
- **冇**反編譯已部署 jar（`06b5b129a114a233`）——只驗 sha 頭 16 位一致；「jar 內含 `stripQuestIcons`」我用 HEAD 原始碼（V16）代替。
- **冇**跑真機（A4／A5 未驗）；**冇**開遊戲、**冇**動沙盒。
- **未能重現** `(758,710,1259)` 簽名（V3 vs V7/V18）——**NOT REPRODUCED**，原因推斷係章節檔喺 run 中途被重寫，但**未證實**（我唔會當成事實）。
- A5 嘅 `adjacentCardPairs` / E9E baseline：**冇查**（唔喺我範圍，亦冇 baseline 檔位置），plan 亦冇提供。

---

## 附錄：狀態空間矩陣（current vs D1）— 逐字輸出

| 輸入 | current | D1 | D1 是否正確 |
|---|---|---|---|
| `icon: { Icon: "a" }` | **CRASH(19>8)** | `''` | ✔（外層整塊剝走） |
| `icon:{Icon:"a"}` | CRASH(15>6) | `''` | ✔ |
| `pre icon: { Count: 1b id: "ftbquests:custom_icon" tag: { Icon: "ftbteams:…" } } post` | CRASH(96>57) | `'pre  post'` | ✔ |
| `icon: "see icon: here"` | CRASH(22>11) | `''` | ✔（字串值本來已丟） |
| `ICON: { Icon: "a" }` | CRASH | `''` | ✔（大小寫不分） |
| `{ icon:{ Icon:"x" } }` | CRASH | `'{  }'` | ✔ |
| `icon: { a: { Icon: "x" } Icon: "y" }` | CRASH | `' item: …'`（尾部保留） | ✔ |
| `icon: { t: "}" Icon: "a" }` | CRASH | 正常 | ✔（字串內 `}` 處理啱） |
| `icon: { Icon: "a" }\r\nicon: "b"\r\n` | CRASH | `'\r\n\r\n'` | ✔（CRLF 本身唔係成因） |
| `'head icon: { Count: 1b tag: { Icon: "a" item: "minecraft:stone" tail icon: "b" end'` | CRASH(82>30) | `'head '` | ✖ **靜默吞尾**（F4） |
| `'head icon: { Count: 1b item: "minecraft:stone" tail'`（無後續 match） | ok → `'head '` | `'head '` | ✖ 同類截斷（既有） |
| `description: ["set icon: { Count: 1b } and icon: x"] item: "minecraft:stone"` | ok → `'description: ["set  and x"] item: …'` | 同 | ✖ 字串被誤剝（既有，D1 唔管） |
| `desc: "text icon: more" item: "minecraft:stone"` | ok → `'desc: "text more" item: …'` | 同 | ✖ 同上（F5） |
| `desc: "see icon:"` | ok → `'desc: "see '` | 同 | ✖ 同上（F5） |
| `icon: "minecraft:stone" tail` | ok → `' tail'` | 同 | ✔ |
| `icon: "a" item: "minecraft:stone" icon: "b"` | ok → `' item: "minecraft:stone" '` | 同 | ✔ |
| `icon:`（位置 0、無值） | ok → `''` | 同 | ✔ |
| `icon: }` | ok → `'}'` | 同 | ✔（key 丟、`}` 留） |
| `icon: 5 item: "minecraft:stone"` | ok → `'5 item: "minecraft:stone"'` | 同 | ✔（key 丟、值留） |
| `icon: { Icon: "a" }   \n\t ` | CRASH | `'   \n\t '` | ✔ |
| `icon: "a\"b" item: "…"`（escape） | ok | 同 | ✔ |
| `icon:\u00a0{ Icon: "a" } item: …`（NBSP） | port: CRASH / **Java: 唔會** | — | ⚠ port 差異（F14） |

> 方法：`strip()` 忠實移植 `QuestGuide.java:1529-1589`（含 `Character.isWhitespace` → Python `isspace()` 之別，見 F14），D1 版 = 同一份 + `if (last > m.start()) continue;`。
