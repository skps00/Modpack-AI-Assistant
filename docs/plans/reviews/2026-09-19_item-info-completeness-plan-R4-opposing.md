# R4 反方 review — item-info-completeness plan（v2 body ＋ §V3 ＋ §V4 覆寫）

- 角色：**反方（adversarial）**。審查對象：`docs/plans/2026-09-19-item-info-completeness-plan.md`（v2 body ＋ §V3 ＋ **§V4**，commit `dfe3a73`）
- 環境（全部親測）：MC **1.19.2** / Forge **43.4.5** / pack **FTB Skies Expert**（`packai_sandbox_ftb`）/ packai `mod_version=0.2.3`
- 證據（本輪親跑）：
  - jar-cache `<ftb sandbox>/minecraft/config/packai/jar-cache/`（**360 檔 = 359 shard ＋ manifest.json**，manifest `jars`=358）
  - `mods/ad_astra-forge-1.19.2-1.12.7.jar`（1,140 worldgen entries 內含 `placed_feature/moon_desh_ore.json` 等）＋ 全 `mods/` 掃描（biome_modifier／`inject/chests/end_city_treasure.json`）
  - 19 條 capable trace `C:/Users/skps9/AppData/Local/Temp/autotest_results_20260919-231137/`（`ask-*.jsonl`）＋ `ask-20260919-231546-ad_astra_oxygen_tank.jsonl`
  - `forge/1.19.2/src/**`（含 50 個 `*Check.java`、`tmp-check.gradle`）、`tests/check_*.py`（123 檔）
- 結論：**正方 5 : 反方 5（go = false）** —— R3 四條 gap 每條都有實質回應（R3 的 linchpin「jar loot ref 由邊個源入 acquire」已鎖定 `JarLightIndex`，係真進步），但 §V4 自己引入 **2 條 CRITICAL**（`dim=` 在現有 parser 中**不存在**；A0 第二個 expected **事實錯**）＋ 3 條 HIGH。
- 本輪只讀；唯一寫入檔＝本報告（原有 **126 項** dirty 未變，新增嘅第 127 項＝本報告 untracked；`forge/`／`neoforge/` 零改動）。

---

## §A 我親核而 §V4 成立／部分成立嘅部分（先講，唔想冤枉）

| §V4 主張 | 我嘅獨立核實 |
|---|---|
| V4.1 資料源＝`JarLightIndex`（jar-cache） | ✅ **方向正確、可行**。`JarLightIndex.byItem`（`:62` `ConcurrentHashMap<String,List<String>>`）真係逐 item 存放**原始 fact code**（`L\|…`／`U\|…`／`R\|…`），由 `loadAllShards`（`:394-433`）＋ `scanMods`（`:287-311`）填；`formatFact`（`:249-271`）已有 `L`→`ReplyLang.jarLoot` 分支 ⇒ `routeLinesForItem` **確實可以純讀 cache、零 jar re-scan**（任務 1a 答案＝**可以**，但見 B3 前置條件） |
| V4.1 命名 fixture `9a59d6f03d4d.json` | ✅ 檔**存在**（359 shard 之一），內容逐字：`jar=ad_astra-forge-1.19.2-1.12.7.jar`、`ad_astra:oxygen_tank → ["L\|chests/village/moon/blacksmith","U\|crafting_shapeless→crafting_shaped\|ad_astra:oxygen_loader"]`；manifest `ad_astra-forge-1.19.2-1.12.7.jar → shard 9a59d6f03d4d.json` ✅ |
| V4.1 expected #1（oxygen_tank ⇒ `chests/village/moon/blacksmith`） | ✅ **有真資料**：`ad_astra-data/ad_astra/loot_tables/chests/village/moon/blacksmith.json` 內文含 `ad_astra:oxygen_tank` ＝ True；cache 亦有該 `L\|` ⇒ 呢半條 expected 站得住 |
| V4.1 合併點存在 | ✅ `AcquireAskTool.java:40-43`（bundle `:40-41` → `clipAcquireLines` `:42` → `return lines.isEmpty() ? "" : join` `:43`）係真 merge seam；`:36-38` 另有 `env==null` 早退（見 B3） |
| V4.1「唔准改 `AskEngine.java:826`」 | ✅ 逐字 `List<String> promptFacts = capable ? List.of() : factsLive;`（`:826`）；keep 呢條禁令正確 |
| V4.2 worldgen 併入 acquire **可行** | ✅ `AskToolEnv` 有 `public final Path gameDir`（`AskToolEnv.java:14`，bind 於 `AskEngine.java:1685`），`WorldgenIndex.lookup(String,Path,String)` 係 public static（`WorldgenIndex.java:47-51`）⇒ acquire **真係拿到世界生成索引同 gameDir**（任務 3 前半答案＝**可以**） |
| V4.2「`worldgen_lookup` 0/19、`acquire` 19/19」 | ✅ 我重跑 trace 直方圖（19 檔／115 次 tool.call）：`jei_lookup 30／render_recipe_cards 29／acquire 19／purpose_lookup 18／quest_fetch 6／guide_fetch 5／item_search 3／tool_build 2／repair_lookup 1／tetra_use 1／consume_use 1`；**`worldgen_lookup` = 0** ✅ |
| V4.2 biome 可出 | ✅ **更正我自己嘅懷疑**：`WorldgenFacts.Store.formatMatches` 有反向鏈（`:639-642`）：`[WORLDGEN] placed_feature ad_astra:moon_desh_ore in biome ad_astra:lunar_wastelands`；實查 ad_astra 18 個 `worldgen/biome/*.json`（`lunar_wastelands` 嘅 features 真含 `ad_astra:moon_desh_ore`）⇒ **`biome=` 有真資料**。`y=`（`height_range` `:429-443`）、`size=`（`configured config.size` `:253-259`）、`count=`（`:278-283`）亦全部可出（我抽真 JSON：`moon_desh_ore` ⇒ count=9、size=9、height -80..80） |
| V4.2「唔加新 tool」避開 schema 閘 | ✅ `tests/check_tool_schema_stable.py:108` `assert forge_only == ["knowledge_lookup"]` 逐字；併入 acquire 確實唔會觸發新紅 |
| V4.3 A3 新「卡＋補充段」fixture 可行 | ✅ `tests/check_ask_card_fallback.py:820-860` 兩個 fixture 都係「prose＋【来源】」形狀，加一段補充文字可測；且 `RecipeEmbed.java:749→764→766→769` 次序 R3 已證 |
| V4.4 prefix 表 **100% 覆蓋** | ✅ 我對 359 shard 全量**獨立重算**：`total refs 19,292／L refs 5,160`（＝plan）；prefix `blocks 2989／chests 727／entities 430／inject 279／gameplay 162／actions 139／archaeology 90／extractor 69／spoils 47／forged 47／misc 42／custom 33／entity 18／elementalcraft 13／advancements 9／chest 3／items 2`；**裸 key 61（7 種）**；`inject` 細分 `inject/chests 169 ＋ inject/entities 87 ＋ inject/<單層> 23`。V3.4 ＋ V4.4 全部 kind 加起來 **= 5,160（100%）** ⇒ 任務 5 答案＝**數值上已 100% 有歸類**（但語意有問題，見 B6） |
| V3.3 基線數字 | ✅ `ls tests/check_*.py` = **123**；`%TEMP%\gate_baseline_20260919.txt` = **123 行**；`tmp-check.gradle:1` 逐字 `// AUTO-GENERATED by research/gen_tmp_check.py`、`:9 runAcquireFactsCheck` ✅ |

判斷：v4 係四輪之中**第一次**把「jar loot ref 由邊個源頭入 acquire」寫死（`JarLightIndex`），亦第一次令 A0 綁到一個**真實存在**嘅 fixture 檔。R3 嘅 linchpin 已被拆。以下係仍然過唔到嘅位。

---

## §B Blockers（每條一個 flip）

### B1（CRITICAL）V4.2 嘅 `W|…|dim=…` 在**現有 parser 中冇任何資料源**，而 A5 已 pre-register「W\| 行含 dim」＋ §V4.2 自己寫「缺欄位唔准填」⇒ **A5 自我矛盾、必然跑唔到**

- 逐字格式：`W|ore|dim=<dim>|biome=<biome>|y=<min>..<max>|size=<n>|count=<n>`。
- **`dim` 全樹不存在**：`grep -i "dimension\|\"dim\"\|dim=" logic/WorldgenFacts.java logic/WorldgenIndex.java` = **0 hits**。`WorldgenFacts` 嘅 7 個 record（`:32-42`：`Biome(id,placedFeatures)`／`Structure(id,biomes)`／`StructureSet(id,structures,spacing,separation)`／`Configured(id,type,size)`／`Placed(id,configuredId,count,countRange,heightRange)`／`Modifier(id,biomes,features)`／Tag）**冇一個有維度欄位**；`formatPlaced`（`:429-443`）只出 `configured=`／`count=`／`height_range=`，`formatConfigured`（`:418-427`）只出 `type=`／`size=`。
- **真 JSON 亦冇**：`ad_astra/worldgen/placed_feature/moon_desh_ore.json` 只有 `feature` ＋ `placement[count,in_square,height_range,biome]`；`configured_feature/moon_desh_ore.json` 只有 `type=minecraft:ore, config.size=9, targets[…]`。**冇 `dimension` 鍵**（1.19.2 worldgen JSON 本身唔帶維度；維度係 datapack 目錄／biome 歸屬嘅外部知識）。
- ⇒ 要靠「`ad_astra` namespace／`lunar_wastelands` 名字 → 推「moon 維度」」＝ **正是 plan 自己禁止嘅 invent**。V4.2 嘅「缺欄位唔准填」同 A5 嘅「含 dim」**在同一節內直接互斥**。（`biome=` 反而係真嘅，見 §A；所以呢條 blocker 精準落喺 `dim` 一字，唔係整條 W 格式。）
- 附帶（同一條）：A5 fixture 之一 **`minecraft:diamond_ore` 唔可能出 `W|` 行** —— `WorldgenIndex` 只掃 `mods/*.jar`（`WorldgenIndex.java:155-205`）＋ loose roots（`:113-127`，`datapacks/kubejs-data/openloader/global_packs/overrides`）。我全 `mods/` 掃描：只有 `archaeology-1.0.0.jar` 帶 `data/minecraft/worldgen/*`，而且**只有 trail_ruins**（20 條，無 `diamond_ore`）。原版 `minecraft:ore_diamond*` 在 **client jar 內，唔在 mods** ⇒ 查 `minecraft:diamond_ore` 得 `WorldgenFacts.missLine`。A5 5 個 fixture 之中至少 1 個**由設計上就綠唔到**。

- **FLIP**（任一成立即撤回）：① 刪走 `dim=` 欄位，改為只斷言**真有資料**嘅欄位（`y`／`size`／`count`／`biome`），並在 A5 逐字寫死「只准呢 4 欄」；**或** ② 明文寫出 dim 嘅**確定性資料源**（例如另立 `D5b`：解析 `biome_modifier`／`dimension` datapack 目錄 → 維度映射；並列入白名單）＋ 公開 1 個 fixture 嘅真值；**或** ③ 把 A5 降級為「有就報、冇就略」嘅描述性項（唔做閘）。同時：把 `minecraft:diamond_ore` 換成**確有 worldgen entry** 嘅礦（實查可用：`ad_astra:moon_desh_ore`／`ad_astra:mars_ostrum_ore`／`ad_astra:glacio_copper_ore`／`ad_astra:venus_diamond_ore`／`ad_astra:mercury_iron_ore`）。

### B2（CRITICAL）A0 第二個 expected（`tetra:dragon_sinew` ⇒ `inject/chests/end_city_treasure`）對住 **plan 自己指名嘅 fixture** 係**事實錯** ⇒ A0 逐字斷言永遠紅

- 我對 359 shard 全量查 `tetra:dragon_sinew`：
  - `0d03c907d03d.json` → `["L|entities/ender_dragon_extended"]`
  - `9607ef2073fb.json` → `["U|crafting_shapeless|art_of_forging:life_fiber"]`
  - **冇任何 `L|inject/chests/end_city_treasure`**。
- `inject/chests/end_city_treasure` 呢個 key **真係存在**，但屬 `artifacts:artifact／artifacts:cloud_in_a_bottle／artifacts:crystal_heart／sophisticatedbackpacks:advanced_magnet_upgrade／voidtotem:totem_of_void_undying` 等 10 件物品，**冇 tetra**。我逐個真 jar 抽該 loot table：`artifacts-1.19.2-5.0.6.jar`／`sophisticatedbackpacks-1.19.2-3.20.2.1035.jar`／`voidtotem-forge-1.19.2-2.1.0.jar` 三份 `…/loot_tables/inject/chests/end_city_treasure.json` 內 `'tetra:dragon_sinew' in text` **全部 = False**。
- 呢個 expected 應該係嚟自 R3 §B1 引嘅**真機 trace**（`Loot table: minecraft:chests/bastion_treasure` ＋ `掉落表「end city treasure」`，源頭係**鬆散 datapack**），唔係 jar-cache。plan 把「真機 trace 見到嘅嘢」誤當成「jar-cache fixture 有嘅嘢」。
- 後果：A0 係 pre-registered「逐字 expected」；其中一條 literally 唔可能成立 ⇒ 實作者會被迫**事後改 expected**（違反 §9「唔准事後改」），或者把 A0 判死。兩種都唔可以收貨。

- **FLIP**：① 把第二條 expected 換成 **fixture 真值**（實查可用：`artifacts:crystal_heart` ⇒ `inject/chests/end_city_treasure`；`tetra:dragon_sinew` ⇒ `entities/ender_dragon_extended`）；**或** ② 若堅持 `tetra:dragon_sinew`，就要另立一個**指名嘅 loose-datapack fixture 檔**（逐字寫出檔內容含 `tetra:dragon_sinew` 同 `inject/chests/end_city_treasure` 呢個 **loot table path**），並同 jar-cache fixture 分開編號（A0-jar／A0-loose）。無論如何：A0 要**明文只准用 jar-cache 真值**做 expected。

### B3（HIGH）A0 斷言「`acquire` 工具回傳文字」，但**冇任何現存 harness 綁得過 `AskToolEnv`**；fixture 前置（manifest／mods 目錄／`scanModJars`）全部未寫 ⇒ A0 嘅「pre-fix 紅」訊號**不可歸因**

- 我 grep 全 `src/test/java/`：`new AskToolEnv(` ＝ **0**、`AskToolLoop.bindEnv(` ＝ **0**；`AskToolEnv` 嘅唯一建構點係 `AskEngine.java:1685`（`bindAskToolEnv`）。現存 50 個 harness 之中最接近嘅 `AcquireFactsCheck` 係**直接 call** `idx.acquireFactsFor(...)`（`:57,:60`）／`acquireFactsDetailed`，**完全繞過 `AcquireAskTool`**。
- 而 `AcquireAskTool.run()` `:36-38`：`AskToolEnv env = AskToolEnv.current(); if (env == null || env.index == null …) return "";`。⇒ 若 harness 淨係 call `new AcquireAskTool().run(args)` 而冇 `AskToolLoop.bindEnv(new AskToolEnv(...))`，**一定回 `""`**，A0 紅 —— 但紅嘅原因係「harness 冇綁 env」，**同「jar loot 有冇到玩家」完全無關**。R3 flip ② 要求嘅「pre-fix 必須紅」係要求**因為真缺陷而紅**，唔係因為 harness 冇 set up。呢條係可證偽性嘅核心（R1-R3 一路講嘅「斷言對象錯／標的錯」）。
- 另外三個唔寫明就一定踩嘅前置：
  1. `JarLightIndex.loadAllShards`（`:394-433`）**只讀 `manifest.json` 列出嘅 shard**（`:395,:407`）⇒ 淨係複製 `9a59d6f03d4d.json` 入 fixture 目錄，**load 唔到**。fixture 必須同時有 `manifest.json`（jar 名→shard）。
  2. `ensure()` 先要 `gameDir/mods` 係目錄（`:74-80`），`factsForAsk` 再要 `PackAiConfig.scanModJars()` 為 true（`:115-117`；寫入側 `:71-73`）⇒ fixture 要有一個（可以空嘅）`mods/`。
  3. **`scanModJars` 預設 = false**（`PackAiConfig.java:442-447` 逐字 `"Default false — safer for huge packs; enable in Mods → Pack AI → Ask."`，`:919-921` accessor）。沙盒係手動開咗（`config/packai-client.toml:140 scanModJars = true`）。plan §6 明令「`config/PackAiConfig.java` **仍唔准郁**」＋ §V4.1 明令「唔准新 config key」⇒ **整條 §V4.1 交付路線係躲在一個預設關閉嘅 client 掣後面**，plan 由頭到尾冇提過。A0b 嘅真機價值完全取決於呢個掣；唔講明就係「驗收條件未寫齊」。
- **FLIP**：① A0 明文寫出 harness 要 `AskToolLoop.bindEnv(...)`（並指出 `AcquireAskTool.java:36-38` 為空 env 早退，所以 env 必須綁），**或** 改為斷言一個**唔靠 env** 嘅 seam（例如 `JarLightIndex.routeLinesForItem` ＋ 一個新 `AcquireMerge` 純函數），令紅／綠可歸因；② fixture 明文包含 `manifest.json` ＋ 空 `mods/` ＋ `scanModJars=true` 嘅設置步驟（並說明 headless 點令 `PackAiConfig` 回 true）；③ plan 明文寫「本交付**前置**：`scanModJars=true`」並在 A0b 寫死呢個前設。

### B4（HIGH）§V4.1 嘅預算約束寫錯，而且**合併次序冇 pin** ⇒ A0 可以因為 `clipLines` 截走 jar ref 而紅／假綠

- plan 逐字：「每 item 上限沿用 `MAX_ACQUIRE_LINES_FULL`」。真值：`AskToolContext.acquireLineBudget(question)`（`:120-122`）= `wantsFullAcquire(question) ? MAX_ACQUIRE_LINES_FULL(12) : MAX_ACQUIRE_LINES_SLIM(3)`；`wantsFullAcquire`（`:115-118`）＝ craft/acquire 導向問題先係 12，其餘 **3**。`AcquireAskTool.java:42` 正是用 `clipAcquireLines(..., args.question)`。⇒ 對一般問題（例如 A0b 嘅氧氣罐問法若唔含 craft/acquire 詞），budget = **3 行**。
- `clipLines`（`:125-143`）語意係「**保留頭 N 行**」。plan 只寫「去重、排序穩定」，**冇寫 jar ref 排前定排後**。若排後（loose 先）而 loose 有 ≥3 行，jar ref 會被整批截走 ⇒ A0 紅，而且係**因為排序而唔係因為交付機制**。
- 同一條仲有：`JarLightIndex` 自身 cap `MAX_ASK_LINES=4`（`:59,:128`）、`MAX_FACTS_PER_ITEM=8`（`:57,:279,:439`）。後者係**掃描期**寫入 `byItem` 時就截（`addFact` `:279`），即「現有 cache map」本身**每 item 最多 8 條 fact code**，`R|/U|` 可能把 `L|` 擠走（entry 按 zip 列舉次序）。`routeLinesForItem` **讀唔到已經被截走嘅嘢** —— 呢點對 A0 兩件 fixture 都 OK（oxygen_tank 2 條、dragon_sinew 2 條），但對「某 item 有 8＋ loot ref」嘅 A1 樣本、以及 A0b 真機，係一個會靜靜漏嘅上限。

- **FLIP**：① 明寫合併次序（建議 **jar ref 先、loose 後**，或「jar ref 永不被 clip」）＋ 明寫「上限＝`acquireLineBudget(question)`（3／12 動態）」而唔係單一常數；② 加一條負控「把 jar ref 排到第 13 位 ⇒ 必紅」以證明排序有被測；③ 明寫 `MAX_FACTS_PER_ITEM=8` 對 `routeLinesForItem` 嘅影響（要唔要另存未截斷版本）。

### B5（MED-HIGH）§V4.3 把 A2/A8 形式化咗，但 (a)「補充段」嘅**宿主檔未指定**、(b) D2 嘅**資料源仍未寫**、(c) A2 嘅 3 行 expected **未 pre-register**

- (a) §V4.3 只寫「插 footer **之前**」。但 §V3.7／R3 已證 footer 切割同派剩卡都係 `RecipeEmbed.java:749→764→766→769`；而 §6 明文「**唔准郁** `logic/RecipeEmbed.java`／`logic/RecipeCard.java`」＋ §V4.5 A3 要求兩者 **sha256 零改動**。⇒ 補充段**一定唔可以**經 RecipeEmbed 入，必須經 `AskEngine`（白名單）或 `AskJeiHints`／`ReplySources`（白名單 8，限「必要時」）。plan **冇寫**係邊個；實作者照字面做會撞正禁令。（`AskJeiHints.java:274` 嘅 `ReplySources.HEADER` anchoring 係現成可用 seam，但 plan 冇指名。）
- (b) §V4.3 逐字「輸入 facts 類別清單 → 輸出『補充』段」。**呢個清單由邊嚟？** V3.1 已判「capable ⇒ `promptFacts` 清空」（`AskEngine.java:826`），R3 §B3 已要求「明寫 D2 資料源＝client 側 facts 組裝（`:481／:686-694` ＋ `loopState.extraFactLines()`）」。§V4.3 **冇做呢件事** ⇒ D2 仍有可能被實作者當成「跟 V3.1 一齊死」。更基本：D2.1 原意係「**逐類檢查答案有冇覆蓋**」，但 V4.3 介面只收「facts 類別清單」，**收唔到答案文字** ⇒ 實作只會變成「有料類別一律照印」。咁 A2 就係**回聲測試**：答案已經講咗嘅類別都會再印一次（重覆），答案完全冇講都會印（補不完）。A2 「3 類有料 ⇒ 輸出 3 行」對呢件事**零鑑別力**。
- (c) A2 逐字寫「輸出必含 **3 行（逐字 expected）**」，但 **3 行係咩字、邊 3 個類別、lang key 名**，全部未寫。§9 明令「每項 pre-registered，唔准事後改」—— expected 留白＝事後任填。

- **FLIP**：① 明寫補充段宿主檔（`AskEngine` 內哪一段，或 `AskJeiHints` anchoring）＋ 明寫 D2 資料源＝client 側 facts 組裝（引 `AskEngine.java:481／:686-694`）；② 明寫 `InfoCompleteness` 介面**包含答案文字**（或者講清係「facts 有料就補，唔理答案」——兩者驗收唔同）；③ A2 貼出 3 行**逐字 expected**（含 lang key 名），並加一條「答案已覆蓋該類 ⇒ 唔准重覆印」嘅斷言（否則 A8 只證明組件存在，唔證明有載荷）。

### B6（MED-HIGH）§V4.4 數值 100% 成立，但三處語意失真：`artifact→artifacts` 改錯、23 條 `inject/<單層>` 未列、`blocks/*`（58%）同**現行 prompt 規則 #22** 正面衝突而 plan 禁止改 prompt

- **`artifact` 改錯**：plan 逐字寫「`artifact`(35) **更正為** `artifacts`」。我真值：裸 key 就係 **`artifact`（35 條）**，來自 `artifacts-1.19.2-5.0.6.jar`（樣本 `artifacts:snorkel`／`artifacts:night_vision_goggles`／`artifacts:panic_necklace`）。寫成 `artifacts` 係**發明一個唔存在嘅 key 字串**（`L|artifacts` 在 359 shard 中 0 條）。若原意係「kind 名叫 artifacts」就要寫清係 kind 而非 key。
- **23 條未列**：`inject/*` 總 279 ＝ `inject/chests 169 ＋ inject/entities 87 ＋` **`inject/<單層> 23`**（`inject/temple_spikes 2`、`inject/temple_bomb`、`inject/stronghold_bomb`、`inject/mineshaft_bomb`、`inject/fortress_bomb`、`inject/temple_blue_bomb`、`inject/stronghold_blue_bomb`、`inject/mineshaft_blue_bomb`、`inject/fortress_blue_bomb`、`inject/dungeon_blue_bomb`、`inject/shipwreck_treasure_globe`、`inject/shipwreck_storage_flax`、`inject/pillager_flax`、`inject/mineshaft_flax`、`inject/dungeon_flax`、`inject/mineshaft_rope`、`inject/mansion_quiver 2`、`inject/dungeon_quiver 2`、`inject/fishing_treasure_antique_ink`、`inject/end_city_stasis`）。呢批落 default `loot_other`，A1「逐 kind 逐字樣本」對佢哋冇 expected。
- **`blocks/*` × prompt 規則 #22**：`blocks/*` = **2,989/5,160 = 58%**（最大單一桶）。plan §V4.4 只講 kind，**完全冇回應 R3 §B4 flip ⑤**（要唔要套 `LootForwardIndex.isTrivialBlockSelfLoot`）。事實：`JarLightIndex.parseLootJson:242` **掃描期已經套咗**（`:242-244`，實作 `LootForwardIndex.java:106-120`：只剔「`blocks/<自己>`」），所以剩下嘅 2,989 條係「唔係方塊掉自己」嘅；但**現行 live prompt 規則 #22**（`packai.reply.loot_noise_skip`，`ReplyLang.java:1235` 拼入 `llmRules`，`LlmClient.java:380/481`）逐字係「Do not cite default block loot (mining a placed block drops itself) as usage or obtain trivia」——即模型被明令**少講方塊掉落**，而 plan 要把**佔 58% 嘅 `blocks/*`** 餵入 acquire，同時 §V4.1 明令「**唔准**改 prompt 文案」。⇒ 呢個最大桶**冇寫明點同 #22 共存**。
- **新噪音通道（§V4 自己引入）**：V4.1 把**全部** L ref 路由入玩家可見嘅 acquire，V4.4 又為全部 61 條裸 key 派 kind ⇒ 以下今日在 cache 存在、pre-V4 **從未到過玩家**（0/19 trace 有 `[JAR]`）嘅字串，post-V4 會變成工具輸出：`L|empty`（2，樣本 `occultism:lighted_air`）、`L|artifact`（35）、`L|loot`（11，樣本來自 ad_astra）、`L|items/drinking_hat`（2）、`L|chest/example_random_source_loot_table`（3）、`L|entity/treasure_goblin`（18，樣本係 `apotheosis:module／minecraft:uniform／placebo:stack_entry`）、`L|advancements/shader_epic`（9）。粗算 **≈127/5,160（2.5%）** 屬掃描噪音；經 `packai.reply.jar_loot = 'loot: %s'`（en_us.json）人化後玩家會見到「`loot: empty`」「`loot: artifact`」「`loot: chest/example_random_source_loot_table`」。另外 V4.4 把 `entity/*→mob_drop`、`advancements/*→loot_other`、`items/*→loot_other` 全部硬派，屬**語意錯標**（`entity/treasure_goblin` 唔係生物掉落表、`advancements/shader_epic` 係成就獎勵表）。
- **FLIP**：① 把「`artifact` 更正為 `artifacts`」改成「`artifact` 係**裸 key**（35 條），唔係 prefix；kind 名稱另行定義」；② 列出 23 條 `inject/<單層>` 並逐條指定 kind（或明文「全部 default `loot_other`，A1 樣本用 X」）；③ 明寫 `blocks/*` 已過 `isTrivialBlockSelfLoot` ＋ 明寫同 prompt 規則 #22 嘅關係（**建議**：jar ref 側唔准出 `blocks/*`，或只出「已通過 #22 例外條件」嘅少數）；④ 加一條**噪音白名單/黑名單**（`empty／artifact／loot／items/…` 等）並寫入 A1 expected，否則 V4 會製造新嘅玩家可見噪音。

### B7（MED）worldgen 併入 acquire 嘅兩個未寫陷阱：`missLine` 洗版 ＋ `WorldgenIndex` 無 disk cache（首次 acquire 會同步掃 357 個 jar）

- `WorldgenIndex.doLookup`（`:82-97`）**查唔到就回一行 miss**：`WorldgenFacts.missLine`（`:91-98`）＝ `[WORLDGEN] this pack has no indexed worldgen for: <q>`（zh 為「此包未索引到 … 的 worldgen」）。若 `acquire` 照樣把 `lookup` 結果 append，**每一件冇 worldgen 嘅物品**都會多一行世界生成 miss ⇒ 直接同 §V4.2「缺欄位唔准填」同「honesty」原則打對台，又會撐爆 B4 嘅行數預算（並令 A0 更易被截）。
- `WorldgenIndex` 註解逐字「Jar scan is capped (**no disk cache**)」（`:18`），`doEnsure`（`:53-…`）每次新 gameDir 就 `scanJars(mods)`（`MAX_JARS=400` `:28`、`MAX_FILES_PER_JAR=250` `:29`）。今日 `WorldgenIndex.lookup` 只在 `WorldgenFacts.looksLikeQuery(question)` 為真時被叫（`AskEngine.java:695-696`）；V4.2 之後 **`acquire`（19/19 被叫）會無條件觸發**，即每個 ask 首次都要同步掃 357 個 jar（對照 `JarLightIndex` 係有 disk cache 先做到「零 re-scan」）。plan 冇任何預算／延遲論述。
- **FLIP**：① 明寫「`acquire` 側**濾走** `WorldgenFacts.missLine`（只 append 真命中行）」；② 明寫 `WorldgenIndex` 掃描成本／觸發時機（或改為只在題目含 worldgen 詞時才 append），並講清要唔要為世界生成加 disk cache（如要＝新工作、要入白名單／版本範圍）。

### B8（LOW-MED）A6 嘅 headless seam 仍未寫（R3 §C-A6 未回應）

- §V4.5 A6 只寫「`guide_fetch` 回傳｜無關查詢（3 個已知 case）｜回空」。目標類真檔 `client/patchouli/PatchouliGuideLookup.java:40` 用 `ForgeRegistries.ITEMS.getKey(stack.getItem())`（client-only registry），R3 已指出 headless 要先明寫 seam（現存先例係經純 logic helper，例如 `PatchouliApiFallbackCheck` 測 `GuidebookPins`）。§V4 由 V3 到 V4 都冇補 ⇒ A6 「形式可證偽、但跑法未定」。另「3 個已知 case」未逐字列出（又一個留白 expected）。
- **FLIP**：明寫 headless seam（call 邊個 pure function）＋ 貼出 3 個 case 嘅逐字 query 同 expected 空回傳。

---

## §C §V4.5 驗收表逐項可證偽性（R4 現況）

| 項 | 可證偽？ | 反方判斷（附 file:line） |
|---|---|---|
| A0 | ⚠️ **否（現狀）** | 斷言對象（`acquire` 工具文字）✅ 明確；fixture 檔真存在 ✅；但 **expected #2 事實錯（B2）**、**env 綁定/manifest/mods/`scanModJars` 未寫（B3）**、**次序＋動態 cap 未 pin（B4）** ⇒ 今日跑 A0 會紅，但**紅因不可歸因** |
| A0b | ⚠️ | 真機、字串明確；但**必然取決於 `scanModJars=true`**（預設 false，`PackAiConfig.java:447`）＋ R3 §C 已提「『掉落表人化名』有兩種人化體（`packai.reply.loot_table_obtain`='Loot table: %s' vs `packai.reply.jar_loot`='loot: %s'）」——V4 仍未 pin 死用邊個 |
| A1 | ⚠️ | 逐字可判、prefix 覆蓋 100%（我已重算）；但**冇貼任何逐字人化樣本**、`artifact` 改錯、23 條未列、`blocks/*` 未回 R3 flip ⑤（B6） |
| A2 | ⚠️ **否（現狀）** | A2 已還原（R3 flip ① 形式滿足）✅；但 **3 行 expected 留白**、**介面收唔到答案文字 ⇒ 回聲化**（B5c/B5b） |
| A3 | ✅ | 舊 fixture ＋ 新「卡＋補充段」fixture（`check_ask_card_fallback.py:820-860` 形狀可承載）；sha256 零改動只證冇郁檔，新 fixture 才係真控制 |
| A5 | ❌ | **`dim` 零資料源 ＋ 同「唔准填缺欄位」互斥（B1）**；且 `minecraft:diamond_ore` 無 worldgen entry（實測只有 archaeology jar 帶 `data/minecraft/worldgen/`，且只 trail_ruins） |
| A6 | ⚠️ | 目標可判，但 headless seam 未寫、3 case 未列（B8） |
| A7 | ✅ | 我親核 forge 50 檔／50 task、python 123 檔＝122 綠＋1 已知紅（`check_ask_display_leak.py`）；`tmp-check.gradle` 屬本地生成檔（`:1` AUTO-GENERATED）⇒ 新 harness **必須**經 `research/gen_tmp_check.py` 重生（V3.6 已入白名單 ✅） |
| A8 | ⚠️ | D0'' 半條：負控「`routeLinesForItem` 回空 ⇒ 紅」**係恆真**（函數回空乜都會紅），唔係 R3 要求嘅「移走該 ref ⇒ 紅」，亦冇鑑別力；D2 半條：形式已指向 A2 ✅，但如 B5c 所述係組件級而非載荷級 |
| coverage 儀器（描述性） | ✅ | 降級正確，基線 75%／90% 我已重現（沿用 R3） |

**仍有一條唔可證偽（A5）＋ 兩條「標的存在但紅因不可歸因」（A0／A8-D0''）＋ 一條 expected 留白（A2）** ⇒ R1 起嘅核心指控**未全清**。

---

## §D §V4 自身引入嘅新矛盾／回歸

1. **`dim=` × 「缺欄位唔准填」**：同一節（§V4.2）內直接互斥；`dim` 在 `WorldgenFacts`／`WorldgenIndex` grep 0 hits（B1）。
2. **A0 expected #2 × 指名 fixture**：plan 自己指定嘅 shard 冇嗰條 `L|`（B2）。
3. **新交付 × 預設關閉 config**：§V4.1 交付路線要 `scanModJars=true`，但 §6 禁郁 `PackAiConfig.java`、§V4.1 禁新 config key ⇒ 呢個前設**永遠唔會由 plan 自己滿足**，而 plan 冇寫明（B3-3）。
4. **`blocks/*`（58%）× prompt 規則 #22 × §V4.1「唔准改 prompt 文案」**：三者互相封死（B6）。
5. **補充段宿主未指定 × §6 RecipeEmbed 禁令 × A3 sha256 零改動**：三條同時成立時，宿主只可以係 `AskEngine`／`AskJeiHints`，但 plan 冇寫（B5a）。
6. **併入 worldgen × 「零 re-scan」賣點**：V4.1 讚 `JarLightIndex` 有 cache，V4.2 卻引入**無 cache** 嘅 `WorldgenIndex` 到每 ask 熱路徑（B7）。
7. **`artifact` → `artifacts`**：`L|artifacts` 在 359 shard = 0 條（B6）。
8. **§V3 未撤回項仍在**：V3.2 嘅「全 corpus 真 item tag id 只有 1 個」錯句（R3 §B5 已證 4 個）在 §V4 冇更正；`A5b`（主包對照）在 §V4.5 表**再次無聲消失**（V3.8 已消失一次，V4 亦冇列）。

---

## §E Flip conditions 總表（每個 blocker 一條）

| # | Blocker | Flip（收窄即翻） |
|---|---|---|
| 1 | B1 `dim` 無源 | 刪 `dim=`／或寫出確定性維度源＋fixture 真值；同時換走 `minecraft:diamond_ore` |
| 2 | B2 A0 expected 事實錯 | 改用 fixture 真值（`artifacts:crystal_heart`／`tetra:dragon_sinew⇒entities/ender_dragon_extended`）或另立指名 loose fixture |
| 3 | B3 紅因不可歸因 | 明寫 `AskToolLoop.bindEnv` 步驟＋fixture 要 `manifest.json`／空 `mods/`／`scanModJars=true`；或改斷言純函數 seam |
| 4 | B4 cap／次序 | 明寫 `acquireLineBudget`（3/12 動態）＋ jar-ref 次序（永不被 clip）＋ 一條排序負控 |
| 5 | B5 D2 宿主／資料源／expected | 明寫宿主檔＋資料源（client facts 組裝）＋貼 A2 三行逐字 expected（含 lang key） |
| 6 | B6 prefix 語意 | 修 `artifact`、列 23 條 `inject/<單層>`、明寫 `blocks/*`×`isTrivialBlockSelfLoot`×prompt #22、加噪音黑名單＋A1 樣本 |
| 7 | B7 worldgen 陷阱 | 明寫濾走 `missLine`＋`WorldgenIndex` 掃描成本／觸發策略 |
| 8 | B8 A6 seam | 明寫 headless seam＋3 個 case 逐字 |

---

## §F 評分、停手報告

- **正方 5 : 反方 5 → go = false**
- **逐輪比分**：R1 2:8 → R2 3:7 → R3 4:6 → **R4 5:5**（連續三輪各 +1；R4 首次鎖定資料源，係真進步，但同時暴露一個**從未有人查過**嘅硬事實：`dim` 在 parser 層根本唔存在）
- **卡死嘅載重決定（兩條，R3 之後只換咗形態冇解決）**：
  1. **`dim`／A5**：V4.2 把 worldgen 交付寫得好具體（格式逐字、fixture 逐個點名），但格式中**唯一無資料源**嘅欄位（dim）正好係 A5 嘅斷言對象；而 plan 自己嘅 anti-invent 原則又禁止補。修法只有三條（刪欄／開新資料源＋白名單／降為描述項），每條都係範圍決定，唔係寫多幾行。
  2. **A0 嘅可歸因性**：斷言對象終於對準 `acquire` 工具文字（對 ✅），但**冇任何 harness 綁得過 `AskToolEnv`**、fixture 前置三件未寫、expected 有一條事實錯 ⇒ 「pre-fix 紅」訊號今日仍然分唔清「真缺陷」同「harness 未 set up」。
- **最貴嘅未知（要拎咩數據才解得開）**：
  1. `dim` 究竟可否由**沙盒實檔**導出（要一次全 `mods/` + loose 掃描，搵 `dimension`／biome-modifier → 維度嘅任何真映射；我今日掃過：`biome_modifier` = 0 檔）。
  2. 一次 **jar-cache-only** A0 在現行 code 下紅嘅**實跑**（我本輪 read-only，只能靠「`PackIndex` 零 jar 掃描」推論 + `AcquireFactsCheck` 用鬆散檔已綠）。
- **按 repo 規則（`AGENTS.md`「Plan／Idea Review 上限 3–4 輪」，R4 已係第 4 輪且仍未達 8:2）→ 立即停手，交 SK 定。** 我嘅建議（推薦 A）：
  - **A（推薦）拆兩件，只放行細嘅嗰件**：
    - **(a)「jar-cache → acquire」單獨出 v5（P0 級）**：範圍＝`JarLightIndex.routeLinesForItem` ＋ `AcquireAskTool` 合併；驗收＝A0（**只 jar-cache 真值**）、A0b（附 `scanModJars=true` 前設）、A1（只 L ref 分類 + 噪音黑名單）、A7。要滿足 §E-2/3/4/6 四條 flip，全部可即時做到。
    - **(b) worldgen（D5/A5）**：連 §E-1 一齊**明文移 P1**（同 tags 一樣處理），因為 `dim` 要新資料源＋白名單變更，唔屬「細修」。
  - **B**：唔拆，硬修 §E-1…8 全部再評一輪 —— 但呢個已經係第 5 輪，**違反 repo 上限**，我唔建議。
  - **C**：照做（接受 `dim` 缺席、A0 expected 事後改、A5 無閘）—— 咁 A5／D2 兩個真機 run 會白做，且 `dim` 缺席會令「維度資訊」承諾落空；我唔建議。

（本輪只讀：無 commit、無 deploy、無開遊戲、未改 `forge/`／`neoforge/`；`git status --porcelain` 仍 126 項。）
