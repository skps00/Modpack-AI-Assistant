# 2026-09-15 — 出貨 0.2.2 準備（內容清單 ＋ 可一鍵執行嘅步驟）

> 狀態：**等 SK 一句**（commit／push 要批准）。本文只記錄「已驗收」同「要點做」。

## 1. 已驗收（Hermes 親跑，非自報）

| 項目 | 證據 |
|---|---|
| Forge compile | `compileJava compileTestJava --rerun-tasks` → RC=0，`error:` 0 |
| Java harness | `gradlew -I tmp-check.gradle run<Name>`：`AskMarkerIntegrityCheck`／`AskReplyScrubCheck`／`AskTraceCheck`／`AskDisplayNameCheck`／`AskDsmlLeakCheck`／`AskCardPlacementCheck`／`AskMissNoticeCheck`／`AskKnowledgeCheck`／`AskKnowledgeRemoteCheck`／`AskKubeJsBridgeCheck`／`AskMechanicFactsCheck`／`ItemRefCheck`／`SettingsLayoutCheck` 全 OK |
| Python 閘 | `tests/check_*.py` 共 **110** 檔，唯一紅＝`check_ask_display_leak`（需真機 log，故意留紅） |
| 雙樹 | `check_dual_tree_sync` PASS；`check_dual_tree_diff_symmetry` `fail=0 warn=3`，`--no-paused` 仍嚴格 FAIL（冇假綠） |
| 負對照（Hermes 親手） | `setters` 刪 1 個 `SPEC.save()` → 指名 FAIL；registry key 改名 → `FAIL: registry path not in PackAiConfig defines`；刪 1 條 zh_tw lang → `FAIL: missing label … in zh_tw.json`；`ControlType.LIST` 中性化 → `FAIL: missing ControlType handler(s)`。全部還原後 md5／`cmp` 一致 |
| 打包 | `gradlew jar` OK；jar 內 lang 3 檔各 484 keys、冇舊 screen class、有 `KnowledgeRemote`／`OfficialDisplay`／`gui/settings/*` |

**Jar（含今日全部修復）**：`forge/1.19.2/build/libs/packai-0.2.1.jar`，sha256 前 16 位 `f3ca96b7329620c4`。

## 2. 今次會 commit 乜（76 項：55 modified ＋ 21 untracked）

* **程式**：`forge/1.19.2/src/main/java/**`（今日：marker 修復、官方名／答案層、DSML、JEI self-IO、KubeJS bridge 診斷、M1d reload invalidate、KB-2 `KnowledgeRemote`、`hasVariantData` 真 bug 修、Settings A/B 全部、review-fix F1–F6）
* **測試／閘**：`forge/...+src/test/java/**`、`tests/*.py`（含新 `check_ask_marker_integrity.py`／`check_official_display_name.py`／`check_settings_registry.py`／`check_settings_setters.py`）
* **資源**：`assets/packai/lang/{en_us,zh_cn,zh_tw}.json`
* **雙樹鏡像**：`neoforge/1.21.1` 只含 `AskEngine`／`AskReplyScrub`／`AskResult`／`ItemResolver`／`ItemRefCheck`／`AskMarkerIntegrityCheck`（邏輯同步；Neo 樹仍 paused）
* **文件**：`docs/plans/*`（含本檔、`2026-09-15_item-marker-integrity-fix.md`、`2026-09-15_real-machine-acceptance.md`）、`README.md`、`code_change_log.md`、`.hermes/plans/HANDOFF.md`
* **刪除**：`client/gui/PackAiSettingsScreen.java`、`client/gui/RecipeCategoryScreen.java`（已被 `SettingsScreenV2`＋`SettingsRegistry` 取代）

**排除（唔准 add）**：`logs/`、`forge/1.19.2/logs/`、`forge/1.19.2/tmp-check.gradle`、`forge/1.19.2/export_tcp.gradle`。

## 3. 步驟（SK 批之後照做）

```bash
# 1) 版本
#    forge/1.19.2/gradle.properties: mod_version=0.2.1 -> 0.2.2（neoforge 暫時維持 0.2.1）
# 2) 明確 add（唔用 -A）
git add docs/plans README.md code_change_log.md .hermes/plans/HANDOFF.md \
        forge/1.19.2/src/main/java forge/1.19.2/src/main/resources \
        forge/1.19.2/src/test/java forge/1.19.2/gradle.properties \
        forge/1.19.2/src/main/java/com/skps9/packai/client/gui/PackAiSettingsScreen.java \
        forge/1.19.2/src/main/java/com/skps9/packai/client/gui/RecipeCategoryScreen.java \
        neoforge/1.21.1/src/main/java neoforge/1.21.1/src/test/java tests
# 3) commit（Conventional Commits）
git commit -m "feat(ask)!: 0.2.2 — marker integrity, official display names, answer layer, KB-2 remote, settings v2, hasVariantData fix"
# 4) push 等 SK 另一句（唔准擅自 push）
```

## 4. 未做（唔可以當完成）

* **真機驗收**（`docs/plans/2026-09-15_real-machine-acceptance.md`）：部署只准 `deploy_packai_jar.py`；要 SK 開 MC 問 5 句；Hermes 讀 trace／`latest.log` 逐項核；`check_ask_display_leak` 應由紅轉綠。
* **Settings C 批**：plan 只寫 S-1／S-2／S-3 有定義，**S-4～S-10 無定義 → 等 SK 清單**。
* **token 瘦身**：要真機 `Pack AI LLM usage prompt=` 真實數字，唔靠估。
* **jar 守門 cron**：等 SK 決定（a 加 cron／b 只寫規則／c 兩樣）。
